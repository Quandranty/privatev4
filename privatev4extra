local kmiWn = {["Frame_1"] = Instance.new("Frame"), ["UICorner_1"] = Instance.new("UICorner"), ["Frame_2"] = Instance.new("Frame"), ["UICorner_2"] = Instance.new("UICorner"), ["TextLabel_1"] = Instance.new("TextLabel"), ["TextButton_1"] = Instance.new("TextButton"), ["UICorner_3"] = Instance.new("UICorner"), ["TextLabel_2"] = Instance.new("TextLabel"), ["Frame_3"] = Instance.new("Frame"), ["UICorner_4"] = Instance.new("UICorner"), ["TextButton_2"] = Instance.new("TextButton"), ["Frame_4"] = Instance.new("Frame"), ["LocalScript_1"] = Instance.new("LocalScript"), ["UICorner_5"] = Instance.new("UICorner"), ["UICorner_6"] = Instance.new("UICorner"), ["TextButton_3"] = Instance.new("TextButton"), ["Frame_5"] = Instance.new("Frame"), ["UICorner_7"] = Instance.new("UICorner"), ["LocalScript_2"] = Instance.new("LocalScript"), ["UICorner_8"] = Instance.new("UICorner"), ["TextButton_4"] = Instance.new("TextButton"), ["Frame_6"] = Instance.new("Frame"), ["UICorner_9"] = Instance.new("UICorner"), ["LocalScript_3"] = Instance.new("LocalScript"), ["UICorner_10"] = Instance.new("UICorner"), ["TextButton_5"] = Instance.new("TextButton"), ["Frame_7"] = Instance.new("Frame"), ["UICorner_11"] = Instance.new("UICorner"), ["LocalScript_4"] = Instance.new("LocalScript"), ["UICorner_12"] = Instance.new("UICorner"), ["TextButton_6"] = Instance.new("TextButton"), ["UICorner_13"] = Instance.new("UICorner"), ["Frame_8"] = Instance.new("Frame"), ["UICorner_14"] = Instance.new("UICorner"), ["LocalScript_5"] = Instance.new("LocalScript"), ["Frame_9"] = Instance.new("Frame"), ["UICorner_15"] = Instance.new("UICorner"), ["TextBox_1"] = Instance.new("TextBox"), ["LocalScript_6"] = Instance.new("LocalScript"), ["Frame_10"] = Instance.new("Frame"), ["UICorner_16"] = Instance.new("UICorner"), ["TextButton_7"] = Instance.new("TextButton"), ["Frame_11"] = Instance.new("Frame"), ["LocalScript_7"] = Instance.new("LocalScript"), ["UICorner_17"] = Instance.new("UICorner"), ["UICorner_18"] = Instance.new("UICorner"), ["TextButton_8"] = Instance.new("TextButton"), ["Frame_12"] = Instance.new("Frame"), ["LocalScript_8"] = Instance.new("LocalScript"), ["UICorner_19"] = Instance.new("UICorner"), ["UICorner_20"] = Instance.new("UICorner"), ["TextButton_9"] = Instance.new("TextButton"), ["Frame_13"] = Instance.new("Frame"), ["LocalScript_9"] = Instance.new("LocalScript"), ["UICorner_21"] = Instance.new("UICorner"), ["UICorner_22"] = Instance.new("UICorner"), ["TextButton_10"] = Instance.new("TextButton"), ["Frame_14"] = Instance.new("Frame"), ["LocalScript_10"] = Instance.new("LocalScript"), ["UICorner_23"] = Instance.new("UICorner"), ["UICorner_24"] = Instance.new("UICorner"), ["TextButton_11"] = Instance.new("TextButton"), ["Frame_15"] = Instance.new("Frame"), ["LocalScript_11"] = Instance.new("LocalScript"), ["UICorner_25"] = Instance.new("UICorner"), ["UICorner_26"] = Instance.new("UICorner"), ["TextButton_12"] = Instance.new("TextButton"), ["Frame_16"] = Instance.new("Frame"), ["LocalScript_12"] = Instance.new("LocalScript"), ["UICorner_27"] = Instance.new("UICorner"), ["UICorner_28"] = Instance.new("UICorner"), ["TextButton_13"] = Instance.new("TextButton"), ["Frame_17"] = Instance.new("Frame"), ["LocalScript_13"] = Instance.new("LocalScript"), ["UICorner_29"] = Instance.new("UICorner"), ["UICorner_30"] = Instance.new("UICorner"), ["TextButton_14"] = Instance.new("TextButton"), ["Frame_18"] = Instance.new("Frame"), ["LocalScript_14"] = Instance.new("LocalScript"), ["UICorner_31"] = Instance.new("UICorner"), ["UICorner_32"] = Instance.new("UICorner"), ["Frame_19"] = Instance.new("Frame"), ["UICorner_33"] = Instance.new("UICorner"), ["ScrollingFrame_1"] = Instance.new("ScrollingFrame"), ["TextButton_15"] = Instance.new("TextButton"), ["Frame_20"] = Instance.new("Frame"), ["LocalScript_15"] = Instance.new("LocalScript"), ["UICorner_34"] = Instance.new("UICorner"), ["UIListLayout_1"] = Instance.new("UIListLayout"), ["LocalScript_16"] = Instance.new("LocalScript"), ["Frame_21"] = Instance.new("Frame"), ["UICorner_35"] = Instance.new("UICorner"), ["TextButton_16"] = Instance.new("TextButton"), ["Frame_22"] = Instance.new("Frame"), ["LocalScript_17"] = Instance.new("LocalScript"), ["UICorner_36"] = Instance.new("UICorner"), ["UICorner_37"] = Instance.new("UICorner"), ["TextButton_17"] = Instance.new("TextButton"), ["Frame_23"] = Instance.new("Frame"), ["LocalScript_18"] = Instance.new("LocalScript"), ["UICorner_38"] = Instance.new("UICorner"), ["UICorner_39"] = Instance.new("UICorner"), ["TextButton_18"] = Instance.new("TextButton"), ["Frame_24"] = Instance.new("Frame"), ["LocalScript_19"] = Instance.new("LocalScript"), ["UICorner_40"] = Instance.new("UICorner"), ["UICorner_41"] = Instance.new("UICorner"), ["TextButton_19"] = Instance.new("TextButton"), ["TextLabel_3"] = Instance.new("TextLabel"), ["UICorner_42"] = Instance.new("UICorner"), ["Frame_25"] = Instance.new("Frame"), ["UICorner_43"] = Instance.new("UICorner"), ["ImageLabel_1"] = Instance.new("ImageLabel"), ["UICorner_44"] = Instance.new("UICorner"), ["LocalScript_20"] = Instance.new("LocalScript"), ["LocalScript_21"] = Instance.new("LocalScript"), ["Frame_26"] = Instance.new("Frame"), ["UICorner_45"] = Instance.new("UICorner"), ["TextButton_20"] = Instance.new("TextButton"), ["Frame_27"] = Instance.new("Frame"), ["UICorner_46"] = Instance.new("UICorner"), ["UICorner_47"] = Instance.new("UICorner"), ["TextButton_21"] = Instance.new("TextButton"), ["Frame_28"] = Instance.new("Frame"), ["UICorner_48"] = Instance.new("UICorner"), ["LocalScript_22"] = Instance.new("LocalScript"), ["UICorner_49"] = Instance.new("UICorner"), ["TextButton_22"] = Instance.new("TextButton"), ["Frame_29"] = Instance.new("Frame"), ["UICorner_50"] = Instance.new("UICorner"), ["UICorner_51"] = Instance.new("UICorner"), ["TextButton_23"] = Instance.new("TextButton"), ["Frame_30"] = Instance.new("Frame"), ["UICorner_52"] = Instance.new("UICorner"), ["UICorner_53"] = Instance.new("UICorner"), ["ScrollingFrame_2"] = Instance.new("ScrollingFrame"), ["TextButton_24"] = Instance.new("TextButton"), ["Frame_31"] = Instance.new("Frame"), ["UICorner_54"] = Instance.new("UICorner"), ["UIListLayout_2"] = Instance.new("UIListLayout"), ["ScrollingFrame_3"] = Instance.new("ScrollingFrame"), ["TextButton_25"] = Instance.new("TextButton"), ["Frame_32"] = Instance.new("Frame"), ["UICorner_55"] = Instance.new("UICorner"), ["UIListLayout_3"] = Instance.new("UIListLayout"), ["LocalScript_23"] = Instance.new("LocalScript"), ["TextLabel_4"] = Instance.new("TextLabel"), ["TextLabel_5"] = Instance.new("TextLabel"), ["Frame_33"] = Instance.new("Frame"), ["UICorner_56"] = Instance.new("UICorner"), ["TextButton_26"] = Instance.new("TextButton"), ["Frame_34"] = Instance.new("Frame"), ["UICorner_57"] = Instance.new("UICorner"), ["LocalScript_24"] = Instance.new("LocalScript"), ["UICorner_58"] = Instance.new("UICorner"), ["TextButton_27"] = Instance.new("TextButton"), ["Frame_35"] = Instance.new("Frame"), ["UICorner_59"] = Instance.new("UICorner"), ["LocalScript_25"] = Instance.new("LocalScript"), ["UICorner_60"] = Instance.new("UICorner"), ["TextButton_28"] = Instance.new("TextButton"), ["Frame_36"] = Instance.new("Frame"), ["LocalScript_26"] = Instance.new("LocalScript"), ["UICorner_61"] = Instance.new("UICorner"), ["UICorner_62"] = Instance.new("UICorner"), ["TextButton_29"] = Instance.new("TextButton"), ["Frame_37"] = Instance.new("Frame"), ["LocalScript_27"] = Instance.new("LocalScript"), ["UICorner_63"] = Instance.new("UICorner"), ["UICorner_64"] = Instance.new("UICorner"), ["TextButton_30"] = Instance.new("TextButton"), ["Frame_38"] = Instance.new("Frame"), ["UICorner_65"] = Instance.new("UICorner"), ["LocalScript_28"] = Instance.new("LocalScript"), ["UICorner_66"] = Instance.new("UICorner"), ["TextButton_31"] = Instance.new("TextButton"), ["Frame_39"] = Instance.new("Frame"), ["LocalScript_29"] = Instance.new("LocalScript"), ["UICorner_67"] = Instance.new("UICorner"), ["UICorner_68"] = Instance.new("UICorner"), ["TextButton_32"] = Instance.new("TextButton"), ["Frame_40"] = Instance.new("Frame"), ["UICorner_69"] = Instance.new("UICorner"), ["LocalScript_30"] = Instance.new("LocalScript"), ["UICorner_70"] = Instance.new("UICorner"), ["TextButton_33"] = Instance.new("TextButton"), ["Frame_41"] = Instance.new("Frame"), ["UICorner_71"] = Instance.new("UICorner"), ["LocalScript_31"] = Instance.new("LocalScript"), ["UICorner_72"] = Instance.new("UICorner"), ["Frame_42"] = Instance.new("Frame"), ["UICorner_73"] = Instance.new("UICorner"), ["ImageLabel_2"] = Instance.new("ImageLabel"), ["UICorner_74"] = Instance.new("UICorner"), ["TextLabel_6"] = Instance.new("TextLabel"), ["LocalScript_32"] = Instance.new("LocalScript"), ["Frame_43"] = Instance.new("Frame"), ["UICorner_75"] = Instance.new("UICorner"), ["TextLabel_7"] = Instance.new("TextLabel"), ["TextLabel_8"] = Instance.new("TextLabel"), ["TextLabel_9"] = Instance.new("TextLabel"), ["IntValue_1"] = Instance.new("IntValue"), ["TextLabel_10"] = Instance.new("TextLabel"), ["IntValue_2"] = Instance.new("IntValue"), ["LocalScript_33"] = Instance.new("LocalScript"), ["LocalScript_34"] = Instance.new("LocalScript"), ["LocalScript_35"] = Instance.new("LocalScript"), ["LocalScript_36"] = Instance.new("LocalScript"), ["LocalScript_37"] = Instance.new("LocalScript"), ["LocalScript_38"] = Instance.new("LocalScript"), ["LocalScript_39"] = Instance.new("LocalScript"), ["Frame_44"] = Instance.new("Frame"), ["UICorner_76"] = Instance.new("UICorner"), ["TextLabel_11"] = Instance.new("TextLabel"), ["LocalScript_40"] = Instance.new("LocalScript"), ["LocalScript_41"] = Instance.new("LocalScript"), ["ScreenGui_1"] = Instance.new("ScreenGui")}

--

kmiWn.ScreenGui_1.Parent = game.CoreGui

----------------------------------------------------------------------------------------------------

kmiWn.Frame_1.BackgroundColor3 = Color3.new(0.196078, 0.196078, 0.196078)
kmiWn.Frame_1.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.Frame_1.BorderSizePixel = 0
kmiWn.Frame_1.Position = UDim2.new(0.303000003, 0, 0, 100)
kmiWn.Frame_1.Size = UDim2.new(0, 797, 0, 427)
kmiWn.Frame_1.Name = [[Main Tab]]
kmiWn.Frame_1.Parent = kmiWn.ScreenGui_1

kmiWn.UICorner_1.Parent = kmiWn.Frame_1

kmiWn.Frame_2.BackgroundColor3 = Color3.new(0.0784314, 0.0784314, 0.0784314)
kmiWn.Frame_2.BorderColor3 = Color3.new(0.0784314, 0.0784314, 0.0784314)
kmiWn.Frame_2.BorderSizePixel = 0
kmiWn.Frame_2.Size = UDim2.new(0, 584, 0, 45)
kmiWn.Frame_2.Name = [[Title Tab]]
kmiWn.Frame_2.Parent = kmiWn.Frame_1

kmiWn.UICorner_2.Parent = kmiWn.Frame_2

kmiWn.TextLabel_1.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Regular, Enum.FontStyle.Normal)
kmiWn.TextLabel_1.Text = [[Medieval Warfare Gui]]
kmiWn.TextLabel_1.TextColor3 = Color3.new(0.901961, 0.901961, 0.901961)
kmiWn.TextLabel_1.TextSize = 30
kmiWn.TextLabel_1.BackgroundColor3 = Color3.new(1, 1, 1)
kmiWn.TextLabel_1.BackgroundTransparency = 1
kmiWn.TextLabel_1.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextLabel_1.BorderSizePixel = 0
kmiWn.TextLabel_1.Size = UDim2.new(0, 306, 0, 45)
kmiWn.TextLabel_1.Name = [[Title]]
kmiWn.TextLabel_1.Parent = kmiWn.Frame_2

kmiWn.TextButton_1.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Light, Enum.FontStyle.Normal)
kmiWn.TextButton_1.Text = [[Push Closure]]
kmiWn.TextButton_1.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_1.TextSize = 23
kmiWn.TextButton_1.BackgroundColor3 = Color3.new(0.666667, 0.333333, 1)
kmiWn.TextButton_1.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_1.BorderSizePixel = 0
kmiWn.TextButton_1.Position = UDim2.new(0.498287678, 0, 0.111111112, 0)
kmiWn.TextButton_1.Size = UDim2.new(0, 128, 0, 34)
kmiWn.TextButton_1.Name = [[PushClosure]]
kmiWn.TextButton_1.Parent = kmiWn.Frame_2

kmiWn.UICorner_3.Parent = kmiWn.TextButton_1

kmiWn.TextLabel_2.FontFace = Font.new("rbxasset://fonts/families/SourceSansPro.json", Enum.FontWeight.Regular, Enum.FontStyle.Italic)
kmiWn.TextLabel_2.Text = [[Discord: pushclosure]]
kmiWn.TextLabel_2.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextLabel_2.TextScaled = true
kmiWn.TextLabel_2.TextSize = 20
kmiWn.TextLabel_2.TextWrapped = true
kmiWn.TextLabel_2.BackgroundColor3 = Color3.new(1, 1, 1)
kmiWn.TextLabel_2.BackgroundTransparency = 1
kmiWn.TextLabel_2.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextLabel_2.BorderSizePixel = 0
kmiWn.TextLabel_2.Position = UDim2.new(0.735126257, 0, 0.17149353, 0)
kmiWn.TextLabel_2.Size = UDim2.new(0, 138, 0, 28)
kmiWn.TextLabel_2.Parent = kmiWn.Frame_2

kmiWn.Frame_3.BackgroundColor3 = Color3.new(0.156863, 0.156863, 0.156863)
kmiWn.Frame_3.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.Frame_3.BorderSizePixel = 0
kmiWn.Frame_3.Position = UDim2.new(-0.000104226914, 0, 0.103585094, 0)
kmiWn.Frame_3.Size = UDim2.new(0, 149, 0, 382)
kmiWn.Frame_3.Name = [[Button Tab]]
kmiWn.Frame_3.Parent = kmiWn.Frame_1

kmiWn.UICorner_4.Parent = kmiWn.Frame_3

kmiWn.TextButton_2.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_2.Text = [[Combat]]
kmiWn.TextButton_2.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_2.TextSize = 16
kmiWn.TextButton_2.BackgroundColor3 = Color3.new(0.235294, 0.235294, 0.235294)
kmiWn.TextButton_2.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_2.BorderSizePixel = 0
kmiWn.TextButton_2.Position = UDim2.new(-0.000134227797, 0, 0.0904712006, 0)
kmiWn.TextButton_2.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_2.ZIndex = 2
kmiWn.TextButton_2.Name = [[Combat]]
kmiWn.TextButton_2.Parent = kmiWn.Frame_3

kmiWn.Frame_4.BackgroundColor3 = Color3.new(0.0784314, 0.0784314, 0.0784314)
kmiWn.Frame_4.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_4.BorderSizePixel = 0
kmiWn.Frame_4.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_4.Name = [[Shadow]]
kmiWn.Frame_4.Parent = kmiWn.TextButton_2

kmiWn.LocalScript_1.Parent = kmiWn.Frame_4

kmiWn.UICorner_5.Parent = kmiWn.Frame_4

kmiWn.UICorner_6.Parent = kmiWn.TextButton_2

kmiWn.TextButton_3.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Regular, Enum.FontStyle.Normal)
kmiWn.TextButton_3.Text = [[Economy]]
kmiWn.TextButton_3.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_3.TextSize = 16
kmiWn.TextButton_3.BackgroundColor3 = Color3.new(0.235294, 0.235294, 0.235294)
kmiWn.TextButton_3.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_3.BorderSizePixel = 0
kmiWn.TextButton_3.Position = UDim2.new(-0.000134227797, 0, 0.311324596, 0)
kmiWn.TextButton_3.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_3.ZIndex = 2
kmiWn.TextButton_3.Name = [[Economy]]
kmiWn.TextButton_3.Parent = kmiWn.Frame_3

kmiWn.Frame_5.BackgroundColor3 = Color3.new(0.0784314, 0.0784314, 0.0784314)
kmiWn.Frame_5.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_5.BorderSizePixel = 0
kmiWn.Frame_5.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_5.Name = [[Shadow]]
kmiWn.Frame_5.Parent = kmiWn.TextButton_3

kmiWn.UICorner_7.Parent = kmiWn.Frame_5

kmiWn.LocalScript_2.Parent = kmiWn.Frame_5

kmiWn.UICorner_8.Parent = kmiWn.TextButton_3

kmiWn.TextButton_4.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Regular, Enum.FontStyle.Normal)
kmiWn.TextButton_4.Text = [[Misc]]
kmiWn.TextButton_4.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_4.TextSize = 16
kmiWn.TextButton_4.BackgroundColor3 = Color3.new(0.235294, 0.235294, 0.235294)
kmiWn.TextButton_4.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_4.BorderSizePixel = 0
kmiWn.TextButton_4.Position = UDim2.new(-0.000134227797, 0, 0.78209424, 0)
kmiWn.TextButton_4.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_4.ZIndex = 2
kmiWn.TextButton_4.Name = [[Misc]]
kmiWn.TextButton_4.Parent = kmiWn.Frame_3

kmiWn.Frame_6.BackgroundColor3 = Color3.new(0.0784314, 0.0784314, 0.0784314)
kmiWn.Frame_6.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_6.BorderSizePixel = 0
kmiWn.Frame_6.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_6.Name = [[Shadow]]
kmiWn.Frame_6.Parent = kmiWn.TextButton_4

kmiWn.UICorner_9.Parent = kmiWn.Frame_6

kmiWn.LocalScript_3.Parent = kmiWn.Frame_6

kmiWn.UICorner_10.Parent = kmiWn.TextButton_4

kmiWn.TextButton_5.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Regular, Enum.FontStyle.Normal)
kmiWn.TextButton_5.Text = [[Bots]]
kmiWn.TextButton_5.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_5.TextSize = 16
kmiWn.TextButton_5.BackgroundColor3 = Color3.new(0.235294, 0.235294, 0.235294)
kmiWn.TextButton_5.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_5.BorderSizePixel = 0
kmiWn.TextButton_5.Position = UDim2.new(-0.00684556365, 0, 0.529738188, 0)
kmiWn.TextButton_5.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_5.ZIndex = 2
kmiWn.TextButton_5.Name = [[Bots]]
kmiWn.TextButton_5.Parent = kmiWn.Frame_3

kmiWn.Frame_7.BackgroundColor3 = Color3.new(0.0784314, 0.0784314, 0.0784314)
kmiWn.Frame_7.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_7.BorderSizePixel = 0
kmiWn.Frame_7.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_7.Name = [[Shadow]]
kmiWn.Frame_7.Parent = kmiWn.TextButton_5

kmiWn.UICorner_11.Parent = kmiWn.Frame_7

kmiWn.LocalScript_4.Parent = kmiWn.Frame_7

kmiWn.UICorner_12.Parent = kmiWn.TextButton_5

kmiWn.TextButton_6.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Regular, Enum.FontStyle.Normal)
kmiWn.TextButton_6.Text = [[Home]]
kmiWn.TextButton_6.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_6.TextSize = 16
kmiWn.TextButton_6.BackgroundColor3 = Color3.new(0.235294, 0.235294, 0.235294)
kmiWn.TextButton_6.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_6.BorderSizePixel = 0
kmiWn.TextButton_6.Position = UDim2.new(-0.000134359274, 0, 0.0151025262, 0)
kmiWn.TextButton_6.Size = UDim2.new(0, 67, 0, 22)
kmiWn.TextButton_6.ZIndex = 2
kmiWn.TextButton_6.Name = [[Home]]
kmiWn.TextButton_6.Parent = kmiWn.Frame_3

kmiWn.UICorner_13.Parent = kmiWn.TextButton_6

kmiWn.Frame_8.BackgroundColor3 = Color3.new(0.0784314, 0.0784314, 0.0784314)
kmiWn.Frame_8.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_8.BorderSizePixel = 0
kmiWn.Frame_8.Size = UDim2.new(0, 67, 0, 23)
kmiWn.Frame_8.Name = [[Shadow]]
kmiWn.Frame_8.Parent = kmiWn.TextButton_6

kmiWn.UICorner_14.Parent = kmiWn.Frame_8

kmiWn.LocalScript_5.Parent = kmiWn.Frame_8

kmiWn.Frame_9.BackgroundColor3 = Color3.new(0.0784314, 0.0784314, 0.0784314)
kmiWn.Frame_9.BorderColor3 = Color3.new(0.0784314, 0.0784314, 0.0784314)
kmiWn.Frame_9.BorderSizePixel = 0
kmiWn.Frame_9.Position = UDim2.new(0.725219667, 0, -5.50316963e-06, 0)
kmiWn.Frame_9.Size = UDim2.new(0, 219, 0, 45)
kmiWn.Frame_9.Name = [[Search Tab]]
kmiWn.Frame_9.Parent = kmiWn.Frame_1

kmiWn.UICorner_15.Parent = kmiWn.Frame_9

kmiWn.TextBox_1.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Bold, Enum.FontStyle.Italic)
kmiWn.TextBox_1.Text = [[Search...]]
kmiWn.TextBox_1.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextBox_1.TextSize = 17
kmiWn.TextBox_1.TextWrapped = true
kmiWn.TextBox_1.BackgroundColor3 = Color3.new(0.176471, 0.176471, 0.176471)
kmiWn.TextBox_1.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextBox_1.BorderSizePixel = 0
kmiWn.TextBox_1.Position = UDim2.new(-0.00038014591, 0, 5.22189657e-05, 0)
kmiWn.TextBox_1.Size = UDim2.new(0, 218, 0, 44)
kmiWn.TextBox_1.ZIndex = 2
kmiWn.TextBox_1.Name = [[Search Bar]]
kmiWn.TextBox_1.Parent = kmiWn.Frame_9

kmiWn.LocalScript_6.Parent = kmiWn.TextBox_1

kmiWn.Frame_10.BackgroundColor3 = Color3.new(0.156863, 0.156863, 0.156863)
kmiWn.Frame_10.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.Frame_10.BorderSizePixel = 0
kmiWn.Frame_10.Position = UDim2.new(0.200752825, 0, 0.12412177, 0)
kmiWn.Frame_10.Size = UDim2.new(0, 408, 0, 363)
kmiWn.Frame_10.Visible = false
kmiWn.Frame_10.Name = [[Combat Tab]]
kmiWn.Frame_10.Parent = kmiWn.Frame_1

kmiWn.UICorner_16.Parent = kmiWn.Frame_10

kmiWn.TextButton_7.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_7.Text = [[Kill Aura]]
kmiWn.TextButton_7.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_7.TextSize = 16
kmiWn.TextButton_7.BackgroundColor3 = Color3.new(0.960784, 0.247059, 0.247059)
kmiWn.TextButton_7.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_7.BorderSizePixel = 0
kmiWn.TextButton_7.Position = UDim2.new(0.0611403212, 0, 0.0739422813, 0)
kmiWn.TextButton_7.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_7.ZIndex = 2
kmiWn.TextButton_7.Name = [[KIllaura]]
kmiWn.TextButton_7.Parent = kmiWn.Frame_10

kmiWn.Frame_11.BackgroundColor3 = Color3.new(0.568627, 0, 0)
kmiWn.Frame_11.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_11.BorderSizePixel = 0
kmiWn.Frame_11.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_11.Name = [[Shadow]]
kmiWn.Frame_11.Parent = kmiWn.TextButton_7

kmiWn.LocalScript_7.Parent = kmiWn.Frame_11

kmiWn.UICorner_17.Parent = kmiWn.Frame_11

kmiWn.UICorner_18.Parent = kmiWn.TextButton_7

kmiWn.TextButton_8.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_8.Text = [[Bow Aura]]
kmiWn.TextButton_8.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_8.TextSize = 16
kmiWn.TextButton_8.BackgroundColor3 = Color3.new(0.960784, 0.247059, 0.247059)
kmiWn.TextButton_8.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_8.BorderSizePixel = 0
kmiWn.TextButton_8.Position = UDim2.new(0.568493247, 0, 0.0739422813, 0)
kmiWn.TextButton_8.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_8.ZIndex = 2
kmiWn.TextButton_8.Name = [[Bowaura]]
kmiWn.TextButton_8.Parent = kmiWn.Frame_10

kmiWn.Frame_12.BackgroundColor3 = Color3.new(0.568627, 0, 0)
kmiWn.Frame_12.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_12.BorderSizePixel = 0
kmiWn.Frame_12.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_12.Name = [[Shadow]]
kmiWn.Frame_12.Parent = kmiWn.TextButton_8

kmiWn.LocalScript_8.Parent = kmiWn.Frame_12

kmiWn.UICorner_19.Parent = kmiWn.Frame_12

kmiWn.UICorner_20.Parent = kmiWn.TextButton_8

kmiWn.TextButton_9.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_9.Text = [[Farm Server (Attach)]]
kmiWn.TextButton_9.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_9.TextSize = 14
kmiWn.TextButton_9.BackgroundColor3 = Color3.new(0.960784, 0.247059, 0.247059)
kmiWn.TextButton_9.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_9.BorderSizePixel = 0
kmiWn.TextButton_9.Position = UDim2.new(0.0611403212, 0, 0.489920288, 0)
kmiWn.TextButton_9.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_9.ZIndex = 2
kmiWn.TextButton_9.Name = [[Farm Server Attach]]
kmiWn.TextButton_9.Parent = kmiWn.Frame_10

kmiWn.Frame_13.BackgroundColor3 = Color3.new(0.568627, 0, 0)
kmiWn.Frame_13.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_13.BorderSizePixel = 0
kmiWn.Frame_13.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_13.Name = [[Shadow]]
kmiWn.Frame_13.Parent = kmiWn.TextButton_9

kmiWn.LocalScript_9.Parent = kmiWn.Frame_13

kmiWn.UICorner_21.Parent = kmiWn.Frame_13

kmiWn.UICorner_22.Parent = kmiWn.TextButton_9

kmiWn.TextButton_10.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_10.Text = [[Farm Server(Safety)]]
kmiWn.TextButton_10.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_10.TextSize = 14
kmiWn.TextButton_10.BackgroundColor3 = Color3.new(0.960784, 0.247059, 0.247059)
kmiWn.TextButton_10.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_10.BorderSizePixel = 0
kmiWn.TextButton_10.Position = UDim2.new(0.568493128, 0, 0.489920288, 0)
kmiWn.TextButton_10.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_10.ZIndex = 2
kmiWn.TextButton_10.Name = [[Farm Server Safety]]
kmiWn.TextButton_10.Parent = kmiWn.Frame_10

kmiWn.Frame_14.BackgroundColor3 = Color3.new(0.568627, 0, 0)
kmiWn.Frame_14.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_14.BorderSizePixel = 0
kmiWn.Frame_14.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_14.Name = [[Shadow]]
kmiWn.Frame_14.Parent = kmiWn.TextButton_10

kmiWn.LocalScript_10.Parent = kmiWn.Frame_14

kmiWn.UICorner_23.Parent = kmiWn.Frame_14

kmiWn.UICorner_24.Parent = kmiWn.TextButton_10

kmiWn.TextButton_11.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_11.Text = [[Farm Ostracize]]
kmiWn.TextButton_11.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_11.TextSize = 16
kmiWn.TextButton_11.BackgroundColor3 = Color3.new(0.960784, 0.247059, 0.247059)
kmiWn.TextButton_11.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_11.BorderSizePixel = 0
kmiWn.TextButton_11.Position = UDim2.new(0.0588957854, 0, 0.279784977, 0)
kmiWn.TextButton_11.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_11.ZIndex = 2
kmiWn.TextButton_11.Name = [[Farm Ostracize]]
kmiWn.TextButton_11.Parent = kmiWn.Frame_10

kmiWn.Frame_15.BackgroundColor3 = Color3.new(0.568627, 0, 0)
kmiWn.Frame_15.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_15.BorderSizePixel = 0
kmiWn.Frame_15.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_15.Name = [[Shadow]]
kmiWn.Frame_15.Parent = kmiWn.TextButton_11

kmiWn.LocalScript_11.Parent = kmiWn.Frame_15

kmiWn.UICorner_25.Parent = kmiWn.Frame_15

kmiWn.UICorner_26.Parent = kmiWn.TextButton_11

kmiWn.TextButton_12.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_12.Text = [[Protect Protectee]]
kmiWn.TextButton_12.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_12.TextSize = 16
kmiWn.TextButton_12.BackgroundColor3 = Color3.new(0.960784, 0.247059, 0.247059)
kmiWn.TextButton_12.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_12.BorderSizePixel = 0
kmiWn.TextButton_12.Position = UDim2.new(0.566248715, 0, 0.279784977, 0)
kmiWn.TextButton_12.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_12.ZIndex = 2
kmiWn.TextButton_12.Name = [[Protect Protectee]]
kmiWn.TextButton_12.Parent = kmiWn.Frame_10

kmiWn.Frame_16.BackgroundColor3 = Color3.new(0.568627, 0, 0)
kmiWn.Frame_16.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_16.BorderSizePixel = 0
kmiWn.Frame_16.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_16.Name = [[Shadow]]
kmiWn.Frame_16.Parent = kmiWn.TextButton_12

kmiWn.LocalScript_12.Parent = kmiWn.Frame_16

kmiWn.UICorner_27.Parent = kmiWn.Frame_16

kmiWn.UICorner_28.Parent = kmiWn.TextButton_12

kmiWn.TextButton_13.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_13.Text = [[Farm Ostracize Bowaura]]
kmiWn.TextButton_13.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_13.TextSize = 14
kmiWn.TextButton_13.BackgroundColor3 = Color3.new(0.960784, 0.247059, 0.247059)
kmiWn.TextButton_13.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_13.BorderSizePixel = 0
kmiWn.TextButton_13.Position = UDim2.new(0.0586891919, 0, 0.696531832, 0)
kmiWn.TextButton_13.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_13.ZIndex = 2
kmiWn.TextButton_13.Name = [[Farm Ostracize Bowaura]]
kmiWn.TextButton_13.Parent = kmiWn.Frame_10

kmiWn.Frame_17.BackgroundColor3 = Color3.new(0.568627, 0, 0)
kmiWn.Frame_17.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_17.BorderSizePixel = 0
kmiWn.Frame_17.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_17.Name = [[Shadow]]
kmiWn.Frame_17.Parent = kmiWn.TextButton_13

kmiWn.LocalScript_13.Parent = kmiWn.Frame_17

kmiWn.UICorner_29.Parent = kmiWn.Frame_17

kmiWn.UICorner_30.Parent = kmiWn.TextButton_13

kmiWn.TextButton_14.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_14.Text = [[Farm Ostracize Safety]]
kmiWn.TextButton_14.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_14.TextSize = 14
kmiWn.TextButton_14.BackgroundColor3 = Color3.new(0.960784, 0.247059, 0.247059)
kmiWn.TextButton_14.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_14.BorderSizePixel = 0
kmiWn.TextButton_14.Position = UDim2.new(0.568493128, 0, 0.696531832, 0)
kmiWn.TextButton_14.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_14.ZIndex = 2
kmiWn.TextButton_14.Name = [[Farm Ostracize Safety]]
kmiWn.TextButton_14.Parent = kmiWn.Frame_10

kmiWn.Frame_18.BackgroundColor3 = Color3.new(0.568627, 0, 0)
kmiWn.Frame_18.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_18.BorderSizePixel = 0
kmiWn.Frame_18.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_18.Name = [[Shadow]]
kmiWn.Frame_18.Parent = kmiWn.TextButton_14

kmiWn.LocalScript_14.Parent = kmiWn.Frame_18

kmiWn.UICorner_31.Parent = kmiWn.Frame_18

kmiWn.UICorner_32.Parent = kmiWn.TextButton_14

kmiWn.Frame_19.BackgroundColor3 = Color3.new(0.137255, 0.137255, 0.137255)
kmiWn.Frame_19.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.Frame_19.BorderSizePixel = 0
kmiWn.Frame_19.Position = UDim2.new(0.725115299, 0, 0, 0)
kmiWn.Frame_19.Size = UDim2.new(0, 219, 0, 421)
kmiWn.Frame_19.Name = [[Players Tab]]
kmiWn.Frame_19.Parent = kmiWn.Frame_1

kmiWn.UICorner_33.Parent = kmiWn.Frame_19

kmiWn.ScrollingFrame_1.CanvasSize = UDim2.new(0, 0, 7, 0)
kmiWn.ScrollingFrame_1.ScrollBarImageColor3 = Color3.new(0, 0, 0)
kmiWn.ScrollingFrame_1.ScrollBarThickness = 0
kmiWn.ScrollingFrame_1.Active = true
kmiWn.ScrollingFrame_1.BackgroundColor3 = Color3.new(0.137255, 0.137255, 0.137255)
kmiWn.ScrollingFrame_1.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.ScrollingFrame_1.BorderSizePixel = 0
kmiWn.ScrollingFrame_1.Position = UDim2.new(0, 0, 0.119999997, 0)
kmiWn.ScrollingFrame_1.Size = UDim2.new(0, 219, 0, 367)
kmiWn.ScrollingFrame_1.Name = [[Player List]]
kmiWn.ScrollingFrame_1.Parent = kmiWn.Frame_19

kmiWn.TextButton_15.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Regular, Enum.FontStyle.Normal)
kmiWn.TextButton_15.Text = [[Player]]
kmiWn.TextButton_15.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_15.TextSize = 17
kmiWn.TextButton_15.BackgroundColor3 = Color3.new(0.176471, 0.176471, 0.176471)
kmiWn.TextButton_15.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_15.BorderSizePixel = 0
kmiWn.TextButton_15.Position = UDim2.new(0.0388127826, 0, 0.110451311, 0)
kmiWn.TextButton_15.Size = UDim2.new(0, 200, 0, 50)
kmiWn.TextButton_15.ZIndex = 2
kmiWn.TextButton_15.Name = [[Player Button Dummy]]
kmiWn.TextButton_15.Parent = kmiWn.ScrollingFrame_1

kmiWn.Frame_20.BackgroundColor3 = Color3.new(0.509804, 0.509804, 0.509804)
kmiWn.Frame_20.BorderColor3 = Color3.new(0.741176, 0.741176, 0.741176)
kmiWn.Frame_20.BorderSizePixel = 0
kmiWn.Frame_20.Size = UDim2.new(0, 200, 0, 53)
kmiWn.Frame_20.Parent = kmiWn.TextButton_15

kmiWn.LocalScript_15.Parent = kmiWn.Frame_20

kmiWn.UICorner_34.Parent = kmiWn.ScrollingFrame_1

kmiWn.UIListLayout_1.Padding = UDim.new(0, 10)
kmiWn.UIListLayout_1.HorizontalAlignment = Enum.HorizontalAlignment.Center
kmiWn.UIListLayout_1.SortOrder = Enum.SortOrder.LayoutOrder
kmiWn.UIListLayout_1.Parent = kmiWn.ScrollingFrame_1

kmiWn.LocalScript_16.Parent = kmiWn.ScrollingFrame_1

kmiWn.Frame_21.BackgroundColor3 = Color3.new(0.196078, 0.196078, 0.196078)
kmiWn.Frame_21.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.Frame_21.BorderSizePixel = 0
kmiWn.Frame_21.Position = UDim2.new(0.0140782185, 0, 1.00140345, 0)
kmiWn.Frame_21.Size = UDim2.new(0, 211, 0, -158)
kmiWn.Frame_21.ZIndex = 2
kmiWn.Frame_21.Name = [[Player Combat]]
kmiWn.Frame_21.Parent = kmiWn.Frame_19

kmiWn.UICorner_35.Parent = kmiWn.Frame_21

kmiWn.TextButton_16.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_16.Text = [[Teleport]]
kmiWn.TextButton_16.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_16.TextSize = 16
kmiWn.TextButton_16.BackgroundColor3 = Color3.new(0.235294, 0.235294, 0.235294)
kmiWn.TextButton_16.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_16.BorderSizePixel = 0
kmiWn.TextButton_16.Position = UDim2.new(0.618094087, 0, 0.297354013, 0)
kmiWn.TextButton_16.Size = UDim2.new(0, 75, 0, 28)
kmiWn.TextButton_16.ZIndex = 3
kmiWn.TextButton_16.Name = [[Teleport]]
kmiWn.TextButton_16.Parent = kmiWn.Frame_21

kmiWn.Frame_22.BackgroundColor3 = Color3.new(0.0784314, 0.0784314, 0.0784314)
kmiWn.Frame_22.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_22.BorderSizePixel = 0
kmiWn.Frame_22.Size = UDim2.new(0, 75, 0, 29)
kmiWn.Frame_22.ZIndex = 2
kmiWn.Frame_22.Name = [[Shadow]]
kmiWn.Frame_22.Parent = kmiWn.TextButton_16

kmiWn.LocalScript_17.Parent = kmiWn.Frame_22

kmiWn.UICorner_36.Parent = kmiWn.Frame_22

kmiWn.UICorner_37.Parent = kmiWn.TextButton_16

kmiWn.TextButton_17.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_17.Text = [[Protectee]]
kmiWn.TextButton_17.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_17.TextSize = 16
kmiWn.TextButton_17.BackgroundColor3 = Color3.new(0.101961, 0.580392, 1)
kmiWn.TextButton_17.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_17.BorderSizePixel = 0
kmiWn.TextButton_17.Position = UDim2.new(-0.00275786337, 0, 0.527417898, 0)
kmiWn.TextButton_17.Size = UDim2.new(0, 210, 0, 28)
kmiWn.TextButton_17.ZIndex = 3
kmiWn.TextButton_17.Name = [[Protectee]]
kmiWn.TextButton_17.Parent = kmiWn.Frame_21

kmiWn.Frame_23.BackgroundColor3 = Color3.new(0.101961, 0.517647, 0.776471)
kmiWn.Frame_23.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_23.BorderSizePixel = 0
kmiWn.Frame_23.Position = UDim2.new(2.90643607e-06, 0, 0, 0)
kmiWn.Frame_23.Size = UDim2.new(0, 210, 0, 30)
kmiWn.Frame_23.ZIndex = 2
kmiWn.Frame_23.Name = [[Shadow]]
kmiWn.Frame_23.Parent = kmiWn.TextButton_17

kmiWn.LocalScript_18.Parent = kmiWn.Frame_23

kmiWn.UICorner_38.Parent = kmiWn.Frame_23

kmiWn.UICorner_39.Parent = kmiWn.TextButton_17

kmiWn.TextButton_18.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_18.Text = [[Ostracize]]
kmiWn.TextButton_18.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_18.TextSize = 16
kmiWn.TextButton_18.BackgroundColor3 = Color3.new(0.960784, 0.247059, 0.247059)
kmiWn.TextButton_18.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_18.BorderSizePixel = 0
kmiWn.TextButton_18.Position = UDim2.new(0.00198031613, 0, 0.785456777, 0)
kmiWn.TextButton_18.Size = UDim2.new(0, 210, 0, 28)
kmiWn.TextButton_18.ZIndex = 3
kmiWn.TextButton_18.Name = [[Ostracize]]
kmiWn.TextButton_18.Parent = kmiWn.Frame_21

kmiWn.Frame_24.BackgroundColor3 = Color3.new(0.568627, 0, 0)
kmiWn.Frame_24.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_24.BorderSizePixel = 0
kmiWn.Frame_24.Position = UDim2.new(1.45321803e-06, 0, 0, 0)
kmiWn.Frame_24.Size = UDim2.new(0, 211, 0, 30)
kmiWn.Frame_24.ZIndex = 2
kmiWn.Frame_24.Name = [[Shadow]]
kmiWn.Frame_24.Parent = kmiWn.TextButton_18

kmiWn.LocalScript_19.Parent = kmiWn.Frame_24

kmiWn.UICorner_40.Parent = kmiWn.Frame_24

kmiWn.UICorner_41.Parent = kmiWn.TextButton_18

kmiWn.TextButton_19.FontFace = Font.new("rbxasset://fonts/families/SourceSansPro.json", Enum.FontWeight.Regular, Enum.FontStyle.Normal)
kmiWn.TextButton_19.Text = [[V]]
kmiWn.TextButton_19.TextColor3 = Color3.new(1, 0.419608, 0.00392157)
kmiWn.TextButton_19.TextSize = 17
kmiWn.TextButton_19.BackgroundColor3 = Color3.new(0.235294, 0.235294, 0.235294)
kmiWn.TextButton_19.BackgroundTransparency = 1
kmiWn.TextButton_19.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_19.BorderSizePixel = 0
kmiWn.TextButton_19.Position = UDim2.new(0.905211508, 0, 0.0026062429, 0)
kmiWn.TextButton_19.Size = UDim2.new(0, 20, 0, 17)
kmiWn.TextButton_19.ZIndex = 2
kmiWn.TextButton_19.Name = [[GoBackButton]]
kmiWn.TextButton_19.Parent = kmiWn.Frame_21

kmiWn.TextLabel_3.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Regular, Enum.FontStyle.Italic)
kmiWn.TextLabel_3.Text = [[frankxz]]
kmiWn.TextLabel_3.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextLabel_3.TextSize = 18
kmiWn.TextLabel_3.TextWrapped = true
kmiWn.TextLabel_3.BackgroundColor3 = Color3.new(0.27451, 0.27451, 0.27451)
kmiWn.TextLabel_3.BackgroundTransparency = 0.30000001192092896
kmiWn.TextLabel_3.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextLabel_3.BorderSizePixel = 0
kmiWn.TextLabel_3.Position = UDim2.new(0.360189587, 0, 0.0290396009, 0)
kmiWn.TextLabel_3.Size = UDim2.new(0, 114, 0, 35)
kmiWn.TextLabel_3.ZIndex = 2
kmiWn.TextLabel_3.Name = [[playerName]]
kmiWn.TextLabel_3.Parent = kmiWn.Frame_21

kmiWn.UICorner_42.Parent = kmiWn.TextLabel_3

kmiWn.Frame_25.BackgroundColor3 = Color3.new(0.235294, 0.235294, 0.235294)
kmiWn.Frame_25.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.Frame_25.BorderSizePixel = 0
kmiWn.Frame_25.Position = UDim2.new(0.0284360182, 0, 0.03164557, 0)
kmiWn.Frame_25.Size = UDim2.new(0, 64, 0, 57)
kmiWn.Frame_25.ZIndex = 2
kmiWn.Frame_25.Parent = kmiWn.Frame_21

kmiWn.UICorner_43.Parent = kmiWn.Frame_25

kmiWn.ImageLabel_1.Image = [[rbxasset://textures/ui/GuiImagePlaceholder.png]]
kmiWn.ImageLabel_1.BackgroundColor3 = Color3.new(1, 1, 1)
kmiWn.ImageLabel_1.BackgroundTransparency = 1
kmiWn.ImageLabel_1.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.ImageLabel_1.BorderSizePixel = 0
kmiWn.ImageLabel_1.Size = UDim2.new(0, 64, 0, 57)
kmiWn.ImageLabel_1.ZIndex = 2
kmiWn.ImageLabel_1.Parent = kmiWn.Frame_25

kmiWn.UICorner_44.Parent = kmiWn.ImageLabel_1

kmiWn.LocalScript_20.Parent = kmiWn.ImageLabel_1

kmiWn.LocalScript_21.Parent = kmiWn.Frame_1

kmiWn.Frame_26.BackgroundColor3 = Color3.new(0.156863, 0.156863, 0.156863)
kmiWn.Frame_26.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.Frame_26.BorderSizePixel = 0
kmiWn.Frame_26.Position = UDim2.new(0.200752825, 0, 0.12412177, 0)
kmiWn.Frame_26.Size = UDim2.new(0, 408, 0, 363)
kmiWn.Frame_26.Visible = false
kmiWn.Frame_26.Name = [[Economy Tab]]
kmiWn.Frame_26.Parent = kmiWn.Frame_1

kmiWn.UICorner_45.Parent = kmiWn.Frame_26

kmiWn.TextButton_20.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_20.Text = [[Auto Farm Tree]]
kmiWn.TextButton_20.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_20.TextSize = 16
kmiWn.TextButton_20.BackgroundColor3 = Color3.new(0.956863, 0.247059, 0.247059)
kmiWn.TextButton_20.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_20.BorderSizePixel = 0
kmiWn.TextButton_20.Position = UDim2.new(0.0170226358, 0, 0.0243555009, 0)
kmiWn.TextButton_20.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_20.ZIndex = 2
kmiWn.TextButton_20.Name = [[Auto Farm Tree]]
kmiWn.TextButton_20.Parent = kmiWn.Frame_26

kmiWn.Frame_27.BackgroundColor3 = Color3.new(0.568627, 0, 0)
kmiWn.Frame_27.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_27.BorderSizePixel = 0
kmiWn.Frame_27.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_27.Name = [[Shadow]]
kmiWn.Frame_27.Parent = kmiWn.TextButton_20

kmiWn.UICorner_46.Parent = kmiWn.Frame_27

kmiWn.UICorner_47.Parent = kmiWn.TextButton_20

kmiWn.TextButton_21.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_21.Text = [[Auto Farm Ore]]
kmiWn.TextButton_21.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_21.TextSize = 16
kmiWn.TextButton_21.BackgroundColor3 = Color3.new(0.956863, 0.247059, 0.247059)
kmiWn.TextButton_21.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_21.BorderSizePixel = 0
kmiWn.TextButton_21.Position = UDim2.new(0.622414768, 0, 0.0243555009, 0)
kmiWn.TextButton_21.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_21.ZIndex = 2
kmiWn.TextButton_21.Name = [[Auto Farm Ore]]
kmiWn.TextButton_21.Parent = kmiWn.Frame_26

kmiWn.Frame_28.BackgroundColor3 = Color3.new(0.568627, 0, 0)
kmiWn.Frame_28.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_28.BorderSizePixel = 0
kmiWn.Frame_28.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_28.Name = [[Shadow]]
kmiWn.Frame_28.Parent = kmiWn.TextButton_21

kmiWn.UICorner_48.Parent = kmiWn.Frame_28

kmiWn.LocalScript_22.Parent = kmiWn.Frame_28

kmiWn.UICorner_49.Parent = kmiWn.TextButton_21

kmiWn.TextButton_22.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_22.Text = [[Whitelist Ore]]
kmiWn.TextButton_22.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_22.TextSize = 16
kmiWn.TextButton_22.BackgroundColor3 = Color3.new(0.956863, 0.247059, 0.247059)
kmiWn.TextButton_22.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_22.BorderSizePixel = 0
kmiWn.TextButton_22.Position = UDim2.new(0.622414768, 0, 0.192399591, 0)
kmiWn.TextButton_22.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_22.ZIndex = 2
kmiWn.TextButton_22.Name = [[Whitelist Ore]]
kmiWn.TextButton_22.Parent = kmiWn.Frame_26

kmiWn.Frame_29.BackgroundColor3 = Color3.new(0.568627, 0, 0)
kmiWn.Frame_29.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_29.BorderSizePixel = 0
kmiWn.Frame_29.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_29.Name = [[Shadow]]
kmiWn.Frame_29.Parent = kmiWn.TextButton_22

kmiWn.UICorner_50.Parent = kmiWn.Frame_29

kmiWn.UICorner_51.Parent = kmiWn.TextButton_22

kmiWn.TextButton_23.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_23.Text = [[Whitelist Tree]]
kmiWn.TextButton_23.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_23.TextSize = 16
kmiWn.TextButton_23.BackgroundColor3 = Color3.new(0.956863, 0.247059, 0.247059)
kmiWn.TextButton_23.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_23.BorderSizePixel = 0
kmiWn.TextButton_23.Position = UDim2.new(0.0170226134, 0, 0.189644769, 0)
kmiWn.TextButton_23.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_23.ZIndex = 2
kmiWn.TextButton_23.Name = [[Whitelist Tree]]
kmiWn.TextButton_23.Parent = kmiWn.Frame_26

kmiWn.Frame_30.BackgroundColor3 = Color3.new(0.568627, 0, 0)
kmiWn.Frame_30.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_30.BorderSizePixel = 0
kmiWn.Frame_30.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_30.Name = [[Shadow]]
kmiWn.Frame_30.Parent = kmiWn.TextButton_23

kmiWn.UICorner_52.Parent = kmiWn.Frame_30

kmiWn.UICorner_53.Parent = kmiWn.TextButton_23

kmiWn.ScrollingFrame_2.CanvasSize = UDim2.new(0, 0, 5, 0)
kmiWn.ScrollingFrame_2.ScrollBarImageColor3 = Color3.new(0, 0, 0)
kmiWn.ScrollingFrame_2.ScrollBarThickness = 5
kmiWn.ScrollingFrame_2.Active = true
kmiWn.ScrollingFrame_2.BackgroundColor3 = Color3.new(1, 1, 1)
kmiWn.ScrollingFrame_2.BackgroundTransparency = 0.8999999761581421
kmiWn.ScrollingFrame_2.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.ScrollingFrame_2.BorderSizePixel = 0
kmiWn.ScrollingFrame_2.Position = UDim2.new(0.0171568636, 0, 0.358127058, 0)
kmiWn.ScrollingFrame_2.Size = UDim2.new(0, 185, 0, 224)
kmiWn.ScrollingFrame_2.Name = [[Tree List]]
kmiWn.ScrollingFrame_2.Parent = kmiWn.Frame_26

kmiWn.TextButton_24.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Regular, Enum.FontStyle.Normal)
kmiWn.TextButton_24.Text = [[Tree]]
kmiWn.TextButton_24.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_24.TextSize = 17
kmiWn.TextButton_24.BackgroundColor3 = Color3.new(0.176471, 0.176471, 0.176471)
kmiWn.TextButton_24.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_24.BorderSizePixel = 0
kmiWn.TextButton_24.Position = UDim2.new(0.0351351351, 0, 0.0099175116, 0)
kmiWn.TextButton_24.Size = UDim2.new(0, 169, 0, 50)
kmiWn.TextButton_24.ZIndex = 2
kmiWn.TextButton_24.Name = [[Tree Dummy]]
kmiWn.TextButton_24.Parent = kmiWn.ScrollingFrame_2

kmiWn.Frame_31.BackgroundColor3 = Color3.new(0.509804, 0.509804, 0.509804)
kmiWn.Frame_31.BorderColor3 = Color3.new(0.741176, 0.741176, 0.741176)
kmiWn.Frame_31.BorderSizePixel = 0
kmiWn.Frame_31.Size = UDim2.new(0, 168, 0, 53)
kmiWn.Frame_31.Parent = kmiWn.TextButton_24

kmiWn.UICorner_54.CornerRadius = UDim.new(0, 0)
kmiWn.UICorner_54.Parent = kmiWn.TextButton_24

kmiWn.UIListLayout_2.Padding = UDim.new(0, 8)
kmiWn.UIListLayout_2.HorizontalAlignment = Enum.HorizontalAlignment.Center
kmiWn.UIListLayout_2.SortOrder = Enum.SortOrder.LayoutOrder
kmiWn.UIListLayout_2.Parent = kmiWn.ScrollingFrame_2

kmiWn.ScrollingFrame_3.CanvasSize = UDim2.new(0, 0, 7, 0)
kmiWn.ScrollingFrame_3.ScrollBarImageColor3 = Color3.new(0, 0, 0)
kmiWn.ScrollingFrame_3.ScrollBarThickness = 5
kmiWn.ScrollingFrame_3.Active = true
kmiWn.ScrollingFrame_3.BackgroundColor3 = Color3.new(1, 1, 1)
kmiWn.ScrollingFrame_3.BackgroundTransparency = 0.8999999761581421
kmiWn.ScrollingFrame_3.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.ScrollingFrame_3.BorderSizePixel = 0
kmiWn.ScrollingFrame_3.Position = UDim2.new(0.514705896, 0, 0.358127058, 0)
kmiWn.ScrollingFrame_3.Size = UDim2.new(0, 185, 0, 224)
kmiWn.ScrollingFrame_3.Name = [[Ore List]]
kmiWn.ScrollingFrame_3.Parent = kmiWn.Frame_26

kmiWn.TextButton_25.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Regular, Enum.FontStyle.Normal)
kmiWn.TextButton_25.Text = [[Ore]]
kmiWn.TextButton_25.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_25.TextSize = 17
kmiWn.TextButton_25.BackgroundColor3 = Color3.new(0.176471, 0.176471, 0.176471)
kmiWn.TextButton_25.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_25.BorderSizePixel = 0
kmiWn.TextButton_25.Position = UDim2.new(0, 0, 0.00472270558, 0)
kmiWn.TextButton_25.Size = UDim2.new(0, 166, 0, 50)
kmiWn.TextButton_25.ZIndex = 2
kmiWn.TextButton_25.Name = [[Ore Dummy]]
kmiWn.TextButton_25.Parent = kmiWn.ScrollingFrame_3

kmiWn.Frame_32.BackgroundColor3 = Color3.new(0.509804, 0.509804, 0.509804)
kmiWn.Frame_32.BorderColor3 = Color3.new(0.741176, 0.741176, 0.741176)
kmiWn.Frame_32.BorderSizePixel = 0
kmiWn.Frame_32.Size = UDim2.new(0, 166, 0, 53)
kmiWn.Frame_32.Parent = kmiWn.TextButton_25

kmiWn.UICorner_55.CornerRadius = UDim.new(0, 0)
kmiWn.UICorner_55.Parent = kmiWn.TextButton_25

kmiWn.UIListLayout_3.Padding = UDim.new(0, 8)
kmiWn.UIListLayout_3.HorizontalAlignment = Enum.HorizontalAlignment.Center
kmiWn.UIListLayout_3.SortOrder = Enum.SortOrder.LayoutOrder
kmiWn.UIListLayout_3.Parent = kmiWn.ScrollingFrame_3

kmiWn.LocalScript_23.Parent = kmiWn.Frame_26

kmiWn.TextLabel_4.FontFace = Font.new("rbxasset://fonts/families/SourceSansPro.json", Enum.FontWeight.Regular, Enum.FontStyle.Normal)
kmiWn.TextLabel_4.TextColor3 = Color3.new(0, 0, 0)
kmiWn.TextLabel_4.TextSize = 14
kmiWn.TextLabel_4.TextTransparency = 1
kmiWn.TextLabel_4.BackgroundColor3 = Color3.new(1, 1, 1)
kmiWn.TextLabel_4.BackgroundTransparency = 1
kmiWn.TextLabel_4.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextLabel_4.BorderSizePixel = 0
kmiWn.TextLabel_4.Position = UDim2.new(0, 0, 1.02754819, 0)
kmiWn.TextLabel_4.Size = UDim2.new(0, 200, 0, 50)
kmiWn.TextLabel_4.Name = [[selectedTree]]
kmiWn.TextLabel_4.Parent = kmiWn.Frame_26

kmiWn.TextLabel_5.FontFace = Font.new("rbxasset://fonts/families/SourceSansPro.json", Enum.FontWeight.Regular, Enum.FontStyle.Normal)
kmiWn.TextLabel_5.TextColor3 = Color3.new(0, 0, 0)
kmiWn.TextLabel_5.TextSize = 14
kmiWn.TextLabel_5.TextTransparency = 1
kmiWn.TextLabel_5.BackgroundColor3 = Color3.new(1, 1, 1)
kmiWn.TextLabel_5.BackgroundTransparency = 1
kmiWn.TextLabel_5.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextLabel_5.BorderSizePixel = 0
kmiWn.TextLabel_5.Position = UDim2.new(0.514705896, 0, 1.030303, 0)
kmiWn.TextLabel_5.Size = UDim2.new(0, 200, 0, 50)
kmiWn.TextLabel_5.Name = [[selectedOre]]
kmiWn.TextLabel_5.Parent = kmiWn.Frame_26

kmiWn.Frame_33.BackgroundColor3 = Color3.new(0.156863, 0.156863, 0.156863)
kmiWn.Frame_33.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.Frame_33.BorderSizePixel = 0
kmiWn.Frame_33.Position = UDim2.new(0.200409144, 0, 0.122496612, 0)
kmiWn.Frame_33.Size = UDim2.new(0, 408, 0, 363)
kmiWn.Frame_33.Visible = false
kmiWn.Frame_33.Name = [[Misc Tab]]
kmiWn.Frame_33.Parent = kmiWn.Frame_1

kmiWn.UICorner_56.Parent = kmiWn.Frame_33

kmiWn.TextButton_26.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_26.Text = [[Steal Flags]]
kmiWn.TextButton_26.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_26.TextSize = 16
kmiWn.TextButton_26.BackgroundColor3 = Color3.new(0.235294, 0.235294, 0.235294)
kmiWn.TextButton_26.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_26.BorderSizePixel = 0
kmiWn.TextButton_26.Position = UDim2.new(0.0341794491, 0, 0.0822067559, 0)
kmiWn.TextButton_26.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_26.ZIndex = 2
kmiWn.TextButton_26.Name = [[Steal Flags]]
kmiWn.TextButton_26.Parent = kmiWn.Frame_33

kmiWn.Frame_34.BackgroundColor3 = Color3.new(0.0784314, 0.0784314, 0.0784314)
kmiWn.Frame_34.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_34.BorderSizePixel = 0
kmiWn.Frame_34.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_34.Name = [[Shadow]]
kmiWn.Frame_34.Parent = kmiWn.TextButton_26

kmiWn.UICorner_57.Parent = kmiWn.Frame_34

kmiWn.LocalScript_24.Parent = kmiWn.Frame_34

kmiWn.UICorner_58.Parent = kmiWn.TextButton_26

kmiWn.TextButton_27.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_27.Text = [[Remove Armor Obj]]
kmiWn.TextButton_27.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_27.TextSize = 16
kmiWn.TextButton_27.BackgroundColor3 = Color3.new(0.235294, 0.235294, 0.235294)
kmiWn.TextButton_27.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_27.BorderSizePixel = 0
kmiWn.TextButton_27.Position = UDim2.new(0.57094413, 0, 0.0822067559, 0)
kmiWn.TextButton_27.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_27.ZIndex = 2
kmiWn.TextButton_27.Name = [[Remove Armor Object]]
kmiWn.TextButton_27.Parent = kmiWn.Frame_33

kmiWn.Frame_35.BackgroundColor3 = Color3.new(0.0784314, 0.0784314, 0.0784314)
kmiWn.Frame_35.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_35.BorderSizePixel = 0
kmiWn.Frame_35.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_35.Name = [[Shadow]]
kmiWn.Frame_35.Parent = kmiWn.TextButton_27

kmiWn.UICorner_59.Parent = kmiWn.Frame_35

kmiWn.LocalScript_25.Parent = kmiWn.Frame_35

kmiWn.UICorner_60.Parent = kmiWn.TextButton_27

kmiWn.TextButton_28.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_28.Text = [[Auto Sack]]
kmiWn.TextButton_28.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_28.TextSize = 16
kmiWn.TextButton_28.BackgroundColor3 = Color3.new(0.956863, 0.247059, 0.247059)
kmiWn.TextButton_28.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_28.BorderSizePixel = 0
kmiWn.TextButton_28.Position = UDim2.new(0.0341793895, 0, 0.759892642, 0)
kmiWn.TextButton_28.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_28.ZIndex = 2
kmiWn.TextButton_28.Name = [[Auto Sack]]
kmiWn.TextButton_28.Parent = kmiWn.Frame_33

kmiWn.Frame_36.BackgroundColor3 = Color3.new(0.568627, 0, 0)
kmiWn.Frame_36.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_36.BorderSizePixel = 0
kmiWn.Frame_36.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_36.Name = [[Shadow]]
kmiWn.Frame_36.Parent = kmiWn.TextButton_28

kmiWn.LocalScript_26.Parent = kmiWn.Frame_36

kmiWn.UICorner_61.Parent = kmiWn.Frame_36

kmiWn.UICorner_62.Parent = kmiWn.TextButton_28

kmiWn.TextButton_29.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_29.Text = [[Auto Chest]]
kmiWn.TextButton_29.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_29.TextSize = 16
kmiWn.TextButton_29.BackgroundColor3 = Color3.new(0.956863, 0.247059, 0.247059)
kmiWn.TextButton_29.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_29.BorderSizePixel = 0
kmiWn.TextButton_29.Position = UDim2.new(0.57094413, 0, 0.305347264, 0)
kmiWn.TextButton_29.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_29.ZIndex = 2
kmiWn.TextButton_29.Name = [[Auto Chest]]
kmiWn.TextButton_29.Parent = kmiWn.Frame_33

kmiWn.Frame_37.BackgroundColor3 = Color3.new(0.568627, 0, 0)
kmiWn.Frame_37.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_37.BorderSizePixel = 0
kmiWn.Frame_37.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_37.Name = [[Shadow]]
kmiWn.Frame_37.Parent = kmiWn.TextButton_29

kmiWn.LocalScript_27.Parent = kmiWn.Frame_37

kmiWn.UICorner_63.Parent = kmiWn.Frame_37

kmiWn.UICorner_64.Parent = kmiWn.TextButton_29

kmiWn.TextButton_30.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_30.Text = [[God Mode]]
kmiWn.TextButton_30.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_30.TextSize = 16
kmiWn.TextButton_30.BackgroundColor3 = Color3.new(0.956863, 0.247059, 0.247059)
kmiWn.TextButton_30.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_30.BorderSizePixel = 0
kmiWn.TextButton_30.Position = UDim2.new(0.0341794491, 0, 0.550526321, 0)
kmiWn.TextButton_30.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_30.ZIndex = 2
kmiWn.TextButton_30.Name = [[God Mode]]
kmiWn.TextButton_30.Parent = kmiWn.Frame_33

kmiWn.Frame_38.BackgroundColor3 = Color3.new(0.568627, 0, 0)
kmiWn.Frame_38.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_38.BorderSizePixel = 0
kmiWn.Frame_38.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_38.Name = [[Shadow]]
kmiWn.Frame_38.Parent = kmiWn.TextButton_30

kmiWn.UICorner_65.Parent = kmiWn.Frame_38

kmiWn.LocalScript_28.Parent = kmiWn.Frame_38

kmiWn.UICorner_66.Parent = kmiWn.TextButton_30

kmiWn.TextButton_31.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_31.Text = [[Auto Pot]]
kmiWn.TextButton_31.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_31.TextSize = 16
kmiWn.TextButton_31.BackgroundColor3 = Color3.new(0.956863, 0.247059, 0.247059)
kmiWn.TextButton_31.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_31.BorderSizePixel = 0
kmiWn.TextButton_31.Position = UDim2.new(0.57094413, 0, 0.547771454, 0)
kmiWn.TextButton_31.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_31.ZIndex = 2
kmiWn.TextButton_31.Name = [[Auto pot]]
kmiWn.TextButton_31.Parent = kmiWn.Frame_33

kmiWn.Frame_39.BackgroundColor3 = Color3.new(0.568627, 0, 0)
kmiWn.Frame_39.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_39.BorderSizePixel = 0
kmiWn.Frame_39.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_39.Name = [[Shadow]]
kmiWn.Frame_39.Parent = kmiWn.TextButton_31

kmiWn.LocalScript_29.Parent = kmiWn.Frame_39

kmiWn.UICorner_67.Parent = kmiWn.Frame_39

kmiWn.UICorner_68.Parent = kmiWn.TextButton_31

kmiWn.TextButton_32.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_32.Text = [[Orbit Outpost]]
kmiWn.TextButton_32.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_32.TextSize = 16
kmiWn.TextButton_32.BackgroundColor3 = Color3.new(0.956863, 0.247059, 0.247059)
kmiWn.TextButton_32.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_32.BorderSizePixel = 0
kmiWn.TextButton_32.Position = UDim2.new(0.570944071, 0, 0.765402317, 0)
kmiWn.TextButton_32.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_32.ZIndex = 2
kmiWn.TextButton_32.Name = [[Orbit Outpost]]
kmiWn.TextButton_32.Parent = kmiWn.Frame_33

kmiWn.Frame_40.BackgroundColor3 = Color3.new(0.568627, 0, 0)
kmiWn.Frame_40.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_40.BorderSizePixel = 0
kmiWn.Frame_40.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_40.Name = [[Shadow]]
kmiWn.Frame_40.Parent = kmiWn.TextButton_32

kmiWn.UICorner_69.Parent = kmiWn.Frame_40

kmiWn.LocalScript_30.Parent = kmiWn.Frame_40

kmiWn.UICorner_70.Parent = kmiWn.TextButton_32

kmiWn.TextButton_33.FontFace = Font.new("rbxasset://fonts/families/Roboto.json", Enum.FontWeight.Medium, Enum.FontStyle.Normal)
kmiWn.TextButton_33.Text = [[Secret Base]]
kmiWn.TextButton_33.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextButton_33.TextSize = 16
kmiWn.TextButton_33.BackgroundColor3 = Color3.new(0.235294, 0.235294, 0.235294)
kmiWn.TextButton_33.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextButton_33.BorderSizePixel = 0
kmiWn.TextButton_33.Position = UDim2.new(0.0341793895, 0, 0.313611656, 0)
kmiWn.TextButton_33.Size = UDim2.new(0, 142, 0, 50)
kmiWn.TextButton_33.ZIndex = 2
kmiWn.TextButton_33.Name = [[Secret Base]]
kmiWn.TextButton_33.Parent = kmiWn.Frame_33

kmiWn.Frame_41.BackgroundColor3 = Color3.new(0.0784314, 0.0784314, 0.0784314)
kmiWn.Frame_41.BorderColor3 = Color3.new(0.117647, 0.117647, 0.117647)
kmiWn.Frame_41.BorderSizePixel = 0
kmiWn.Frame_41.Size = UDim2.new(0, 142, 0, 53)
kmiWn.Frame_41.Name = [[Shadow]]
kmiWn.Frame_41.Parent = kmiWn.TextButton_33

kmiWn.UICorner_71.Parent = kmiWn.Frame_41

kmiWn.LocalScript_31.Parent = kmiWn.Frame_41

kmiWn.UICorner_72.Parent = kmiWn.TextButton_33

kmiWn.Frame_42.BackgroundColor3 = Color3.new(0.156863, 0.156863, 0.156863)
kmiWn.Frame_42.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.Frame_42.BorderSizePixel = 0
kmiWn.Frame_42.Position = UDim2.new(0.200752825, 0, 0.12412177, 0)
kmiWn.Frame_42.Size = UDim2.new(0, 408, 0, 363)
kmiWn.Frame_42.Name = [[Off Tab]]
kmiWn.Frame_42.Parent = kmiWn.Frame_1

kmiWn.UICorner_73.Parent = kmiWn.Frame_42

kmiWn.ImageLabel_2.Image = [[rbxasset://textures/ui/GuiImagePlaceholder.png]]
kmiWn.ImageLabel_2.BackgroundColor3 = Color3.new(1, 1, 1)
kmiWn.ImageLabel_2.BackgroundTransparency = 1
kmiWn.ImageLabel_2.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.ImageLabel_2.BorderSizePixel = 0
kmiWn.ImageLabel_2.Position = UDim2.new(0.0311983898, 0, 0.0566318817, 0)
kmiWn.ImageLabel_2.Size = UDim2.new(0, 109, 0, 109)
kmiWn.ImageLabel_2.Parent = kmiWn.Frame_42

kmiWn.UICorner_74.CornerRadius = UDim.new(1, 0)
kmiWn.UICorner_74.Parent = kmiWn.ImageLabel_2

kmiWn.TextLabel_6.FontFace = Font.new("rbxasset://fonts/families/SourceSansPro.json", Enum.FontWeight.Bold, Enum.FontStyle.Italic)
kmiWn.TextLabel_6.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextLabel_6.TextSize = 20
kmiWn.TextLabel_6.BackgroundColor3 = Color3.new(1, 1, 1)
kmiWn.TextLabel_6.BackgroundTransparency = 1
kmiWn.TextLabel_6.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextLabel_6.BorderSizePixel = 0
kmiWn.TextLabel_6.Position = UDim2.new(1.33270991, 0, 0.259615391, 0)
kmiWn.TextLabel_6.Size = UDim2.new(0, 226, 0, 50)
kmiWn.TextLabel_6.Name = [[playerName]]
kmiWn.TextLabel_6.Parent = kmiWn.ImageLabel_2

kmiWn.LocalScript_32.Parent = kmiWn.ImageLabel_2

kmiWn.Frame_43.BackgroundColor3 = Color3.new(0.0784314, 0.0784314, 0.0784314)
kmiWn.Frame_43.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.Frame_43.BorderSizePixel = 0
kmiWn.Frame_43.Position = UDim2.new(0, 0, 0.449467838, 0)
kmiWn.Frame_43.Size = UDim2.new(0, 408, 0, 199)
kmiWn.Frame_43.Parent = kmiWn.Frame_42

kmiWn.UICorner_75.Parent = kmiWn.Frame_43

kmiWn.TextLabel_7.FontFace = Font.new("rbxasset://fonts/families/SourceSansPro.json", Enum.FontWeight.Regular, Enum.FontStyle.Italic)
kmiWn.TextLabel_7.Text = [[Trees Chopped: ]]
kmiWn.TextLabel_7.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextLabel_7.TextScaled = true
kmiWn.TextLabel_7.TextSize = 20
kmiWn.TextLabel_7.TextWrapped = true
kmiWn.TextLabel_7.BackgroundColor3 = Color3.new(1, 1, 1)
kmiWn.TextLabel_7.BackgroundTransparency = 1
kmiWn.TextLabel_7.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextLabel_7.BorderSizePixel = 0
kmiWn.TextLabel_7.Position = UDim2.new(0.048851762, 0, 0.121242352, 0)
kmiWn.TextLabel_7.Size = UDim2.new(0, 138, 0, 28)
kmiWn.TextLabel_7.Name = [[TreesChopped]]
kmiWn.TextLabel_7.Parent = kmiWn.Frame_43

kmiWn.TextLabel_8.FontFace = Font.new("rbxasset://fonts/families/SourceSansPro.json", Enum.FontWeight.Regular, Enum.FontStyle.Italic)
kmiWn.TextLabel_8.Text = [[Ores Mined:]]
kmiWn.TextLabel_8.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextLabel_8.TextScaled = true
kmiWn.TextLabel_8.TextSize = 20
kmiWn.TextLabel_8.TextWrapped = true
kmiWn.TextLabel_8.BackgroundColor3 = Color3.new(1, 1, 1)
kmiWn.TextLabel_8.BackgroundTransparency = 1
kmiWn.TextLabel_8.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextLabel_8.BorderSizePixel = 0
kmiWn.TextLabel_8.Position = UDim2.new(0.048851762, 0, 0.633805156, 0)
kmiWn.TextLabel_8.Size = UDim2.new(0, 138, 0, 28)
kmiWn.TextLabel_8.Name = [[OresMined]]
kmiWn.TextLabel_8.Parent = kmiWn.Frame_43

kmiWn.TextLabel_9.FontFace = Font.new("rbxasset://fonts/families/SourceSansPro.json", Enum.FontWeight.Regular, Enum.FontStyle.Italic)
kmiWn.TextLabel_9.Text = [[0]]
kmiWn.TextLabel_9.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextLabel_9.TextScaled = true
kmiWn.TextLabel_9.TextSize = 20
kmiWn.TextLabel_9.TextWrapped = true
kmiWn.TextLabel_9.BackgroundColor3 = Color3.new(1, 1, 1)
kmiWn.TextLabel_9.BackgroundTransparency = 1
kmiWn.TextLabel_9.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextLabel_9.BorderSizePixel = 0
kmiWn.TextLabel_9.Position = UDim2.new(0.4434596, 0, 0.0961167216, 0)
kmiWn.TextLabel_9.Size = UDim2.new(0, 150, 0, 28)
kmiWn.TextLabel_9.Name = [[ChoppedValue]]
kmiWn.TextLabel_9.Parent = kmiWn.Frame_43

kmiWn.IntValue_1.Name = [[chopval]]
kmiWn.IntValue_1.Parent = kmiWn.TextLabel_9

kmiWn.TextLabel_10.FontFace = Font.new("rbxasset://fonts/families/SourceSansPro.json", Enum.FontWeight.Regular, Enum.FontStyle.Italic)
kmiWn.TextLabel_10.Text = [[0]]
kmiWn.TextLabel_10.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextLabel_10.TextScaled = true
kmiWn.TextLabel_10.TextSize = 20
kmiWn.TextLabel_10.TextWrapped = true
kmiWn.TextLabel_10.BackgroundColor3 = Color3.new(1, 1, 1)
kmiWn.TextLabel_10.BackgroundTransparency = 1
kmiWn.TextLabel_10.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextLabel_10.BorderSizePixel = 0
kmiWn.TextLabel_10.Position = UDim2.new(0.4434596, 0, 0.65390569, 0)
kmiWn.TextLabel_10.Size = UDim2.new(0, 150, 0, 28)
kmiWn.TextLabel_10.Name = [[MinedValue]]
kmiWn.TextLabel_10.Parent = kmiWn.Frame_43

kmiWn.IntValue_2.Name = [[mineval]]
kmiWn.IntValue_2.Parent = kmiWn.TextLabel_10

kmiWn.LocalScript_33.Parent = kmiWn.Frame_43

kmiWn.LocalScript_34.Name = [[globalVariables]]
kmiWn.LocalScript_34.Parent = kmiWn.Frame_1

kmiWn.LocalScript_35.Name = [[Protect]]
kmiWn.LocalScript_35.Parent = kmiWn.Frame_1

kmiWn.LocalScript_36.Name = [[OwnerAdmin]]
kmiWn.LocalScript_36.Parent = kmiWn.Frame_1

kmiWn.LocalScript_37.Name = [[kickscript]]
kmiWn.LocalScript_37.Parent = kmiWn.Frame_1

kmiWn.LocalScript_38.Parent = kmiWn.Frame_1

kmiWn.LocalScript_39.Name = [[AnticheatBypass]]
kmiWn.LocalScript_39.Parent = kmiWn.Frame_1

kmiWn.Frame_44.BackgroundColor3 = Color3.new(0.156863, 0.156863, 0.156863)
kmiWn.Frame_44.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.Frame_44.BorderSizePixel = 0
kmiWn.Frame_44.Position = UDim2.new(0.200409144, 0, 0.124838531, 0)
kmiWn.Frame_44.Size = UDim2.new(0, 408, 0, 363)
kmiWn.Frame_44.Visible = false
kmiWn.Frame_44.Name = [[Bots Tab]]
kmiWn.Frame_44.Parent = kmiWn.Frame_1

kmiWn.UICorner_76.Parent = kmiWn.Frame_44

kmiWn.TextLabel_11.FontFace = Font.new("rbxasset://fonts/families/SourceSansPro.json", Enum.FontWeight.Regular, Enum.FontStyle.Normal)
kmiWn.TextLabel_11.Text = [[Only works for rawnull.]]
kmiWn.TextLabel_11.TextColor3 = Color3.new(1, 1, 1)
kmiWn.TextLabel_11.TextSize = 43
kmiWn.TextLabel_11.TextWrapped = true
kmiWn.TextLabel_11.BackgroundColor3 = Color3.new(0.231373, 0.231373, 0.231373)
kmiWn.TextLabel_11.BorderColor3 = Color3.new(0, 0, 0)
kmiWn.TextLabel_11.BorderSizePixel = 0
kmiWn.TextLabel_11.Position = UDim2.new(0.0514705889, 0, 0.0514984541, 0)
kmiWn.TextLabel_11.Size = UDim2.new(0, 375, 0, 320)
kmiWn.TextLabel_11.Parent = kmiWn.Frame_44

kmiWn.LocalScript_40.Parent = kmiWn.Frame_1

kmiWn.LocalScript_41.Parent = kmiWn.Frame_1

kmiWn.ScreenGui_1.Name = [[Control Panel]]
----------------------------------------------------------------------------------------------------

local function LocalScript_1(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local ButtonTab = Combat.Parent
	local TweenService = game:GetService("TweenService")
	local pressed = false


	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local ShadowTweenLeave = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(20, 20, 20)}
	)

	local CombatTweenLeave = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(60,60,60)}
	)

	Combat.MouseButton1Click:Connect(function()
		CombatTween:Play()
		ShadowTween:Play()
		Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
		Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)

		for i,v in pairs(ButtonTab:GetChildren()) do
			if v:IsA("TextButton") then
				if v.Name ~= Combat.Parent.Name then
					v.BackgroundColor3 = Color3.fromRGB(60,60,60)
					for i,c in pairs(v:GetChildren()) do
						if c:IsA("Frame") and c.Name == "Shadow" then
							c.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
						end
					end
				end
			end
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_27(script)

	local ChestNodes = game.Workspace.Ignore["Chest Nodes"]
	local Communication = game:GetService("ReplicatedStorage").Communication
	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local TweenService = game:GetService("TweenService")
	local ChestNodes = game.Workspace.Ignore["Chest Nodes"]
	local Communication = game:GetService("ReplicatedStorage").Communication
	local pressed = false

	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local CombatTweenClick = TweenService:Create(
		Combat,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(244,63,63)}
	)
	local ShadowTweenClick = TweenService:Create(
		Shadow,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(145,0,0)}

	)

	Combat.MouseButton1Click:Connect(function()
		if not pressed then
			pressed = true
			CombatTween:Play()
			ShadowTween:Play()
			wait(0.5)
			Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
			Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)
			local function updateChest()
				local oldpos = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
				for i,v in pairs(ChestNodes:GetChildren()) do
					if not string.find(v.Name, "sack") then
						print("Found")
						local char = game.Players.LocalPlayer.Character or game.Players.LocalPlayer.CharacterAdded:Wait()
						char.HumanoidRootPart.CFrame = v.CFrame
						task.wait(0.2)
						Communication.PurchaseWorldChest:InvokeServer(tonumber(v.Name))
						task.wait(0.2)
						char.HumanoidRootPart.CFrame = oldpos
					end
				end
			end

			updateChest()

			ChestNodes.ChildAdded:Connect(function()
				for i,v in pairs(ChestNodes:GetChildren()) do
					if not string.find("sack",v.Name) then
						updateChest()
					end
				end
			end)
		else
			pressed = false
			ShadowTweenClick:Play()
			CombatTweenClick:Play()
			wait(0.5)
			Combat.BackgroundColor3 = Color3.fromRGB(244,63,63)
			Shadow.BackgroundColor3 = Color3.fromRGB(145,0,0)
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_28(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local TweenService = game:GetService("TweenService")
	local pressed = false

	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local CombatTweenClick = TweenService:Create(
		Combat,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(244,63,63)}
	)
	local ShadowTweenClick = TweenService:Create(
		Shadow,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(145,0,0)}

	)

	Combat.MouseButton1Click:Connect(function()
		if not pressed then
			pressed = true
			_G.FALL = false
			CombatTween:Play()
			ShadowTween:Play()
			wait(0.5)
			Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
			Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)
			local Players = game:GetService("Players")
			local player = Players.LocalPlayer
			local Character, Humanoid, HumanoidRootPart
			local OldPos
			local function assign()
				local Character = player.Character
				if Character and Character.Parent ~= nil then
					Humanoid = Character:WaitForChild("Humanoid")
					HumanoidRootPart = Character:WaitForChild("HumanoidRootPart")
				end
			end
			assign()
			player.CharacterAdded:Connect(assign)

			while _G.FALL == false do
				wait()
				Character = player.Character
				Humanoid = Character:WaitForChild("Humanoid")
				HumanoidRootPart = Character:WaitForChild("HumanoidRootPart")
				Humanoid.HealthChanged:Connect(function()
					local LowHealthSlow = Character:WaitForChild("LowHealthSlow")
					for i, v in pairs(getconnections(LowHealthSlow:GetPropertyChangedSignal("Disabled"))) do
						v:Disable()
					end
					LowHealthSlow.Disabled = true
					Humanoid:SetStateEnabled(Enum.HumanoidStateType.Dead, false)
					if Humanoid.Health == 0 then
						task.wait(4.9)
						OldPos = HumanoidRootPart.CFrame
						task.wait(0.2)
						HumanoidRootPart.CFrame = OldPos

					end
				end)
			end

		else
			pressed = false
			ShadowTweenClick:Play()
			CombatTweenClick:Play()
			wait(0.5)
			Combat.BackgroundColor3 = Color3.fromRGB(244,63,63)
			Shadow.BackgroundColor3 = Color3.fromRGB(145,0,0)
			_G.FALL = true
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_29(script)

	local ReplicatedStorage = game:GetService("ReplicatedStorage")
	local Players = game:GetService("Players")
	local Client = Players.LocalPlayer
	local Communication = ReplicatedStorage.Communication
	local ClientData = ReplicatedStorage.Player_Data[Client.UserId].Save_Data
	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local TweenService = game:GetService("TweenService")
	local pressed = false
	local ClientHRP, clientHumanoid

	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local CombatTweenClick = TweenService:Create(
		Combat,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(244,63,63)}
	)
	local ShadowTweenClick = TweenService:Create(
		Shadow,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(145,0,0)}

	)

	Combat.MouseButton1Click:Connect(function()
		if not pressed then
			pressed = true
			CombatTween:Play()
			ShadowTween:Play()
			wait(0.5)
			Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
			Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)
			local LocalPlayer = game:GetService("Players").LocalPlayer
			LocalPlayer.Character.Humanoid:GetPropertyChangedSignal("Health"):Connect(function()
				if LocalPlayer.Character.Humanoid.Health <= 70 then
					Communication.UsePotion:InvokeServer(3)
				end
			end)

		else
			pressed = false
			ShadowTweenClick:Play()
			CombatTweenClick:Play()
			wait(0.5)
			Combat.BackgroundColor3 = Color3.fromRGB(244,63,63)
			Shadow.BackgroundColor3 = Color3.fromRGB(145,0,0)
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_30(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local TweenService = game:GetService("TweenService")
	local pressed = false

	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local CombatTweenClick = TweenService:Create(
		Combat,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(244,63,63)}
	)
	local ShadowTweenClick = TweenService:Create(
		Shadow,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(145,0,0)}

	)

	Combat.MouseButton1Click:Connect(function()
		_G.T = true
		if not pressed then
			pressed = true
			CombatTween:Play()
			ShadowTween:Play()
			wait(0.5)
			Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
			Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)


			local Players = game:GetService("Players")
			local RunService = game:GetService("RunService")
			local Client = Players.LocalPlayer
			local playerselected = nil

			local orbiting = false

			local Character,Humanoid,RootPart
			local function SetCharVars()
				Character = Client.Character
				Humanoid = Character:WaitForChild("Humanoid")
				RootPart = Character:WaitForChild("HumanoidRootPart")
			end
			SetCharVars()

			Client.CharacterAdded:Connect(SetCharVars)

			local part1 = Instance.new('Part')
			part1.Anchored = true
			part1.Position = Vector3.new(608, 370, -93)
			part1.Size = Vector3.new (100,0.1,100)
			part1.Parent = workspace
			part1.Name = "base"
			part1.Transparency = 1
			local moon = RootPart
			while _G.T == true do
				wait()
				local ORBIT_TIME = 1
				local RADIUS = 60
				local ECLIPSE = 1
				local ROTATION = CFrame.Angles(0,0,0) 

				-- But not these
				local sin, cos = math.sin, math.cos
				local ROTSPEED = math.pi*2/ORBIT_TIME
				ECLIPSE = ECLIPSE * RADIUS
				local runservice = game:GetService('RunService')

				-- There are many ways to run this loop, you could do while true do if you want, if so use this: rot = rot + wait() * ROTSPEED 
				local rot = 0
				while _G.T == true do
					rot = rot + wait() * ROTSPEED
					moon.CFrame = ROTATION * CFrame.new(sin(rot)*ECLIPSE, 0, cos(rot)*RADIUS) + Vector3.new(part1.Position.X, part1.Position.Y + 6.7, part1.Position.Z)
				end
			end


		else
			pressed = false
			ShadowTweenClick:Play()
			CombatTweenClick:Play()
			wait(0.5)
			Combat.BackgroundColor3 = Color3.fromRGB(244,63,63)
			Shadow.BackgroundColor3 = Color3.fromRGB(145,0,0)	
			_G.T = false
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_26(script)

	local ChestNodes = game.Workspace.Ignore["Chest Nodes"]
	local Communication = game:GetService("ReplicatedStorage").Communication
	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local TweenService = game:GetService("TweenService")
	local ChestNodes = game.Workspace.Ignore["Chest Nodes"]
	local Communication = game:GetService("ReplicatedStorage").Communication
	local pressed = false

	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local CombatTweenClick = TweenService:Create(
		Combat,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(244,63,63)}
	)
	local ShadowTweenClick = TweenService:Create(
		Shadow,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(145,0,0)}

	)

	Combat.MouseButton1Click:Connect(function()
		if not pressed then
			pressed = true
			CombatTween:Play()
			ShadowTween:Play()
			wait(0.5)
			Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
			Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)
			local function updateSack()
				for i,v in pairs(ChestNodes:GetChildren()) do
					if string.find(v.Name, "sack") then
						print("Found")
						local char = game.Players.LocalPlayer.Character or game.Players.LocalPlayer.CharacterAdded:Wait()
						local oldpos = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
						task.wait(0.2)
						char.HumanoidRootPart.CFrame = v.CFrame
						task.wait(0.2)
						char.HumanoidRootPart.CFrame = oldpos
					end
				end
			end

			updateSack()

			ChestNodes.ChildAdded:Connect(function()
				for i,v in pairs(ChestNodes:GetChildren()) do
					if string.find("sack",v.Name) then
						updateSack()
					end
				end
			end)
		else
			pressed = false
			ShadowTweenClick:Play()
			CombatTweenClick:Play()
			wait(0.5)
			Combat.BackgroundColor3 = Color3.fromRGB(244,63,63)
			Shadow.BackgroundColor3 = Color3.fromRGB(145,0,0)
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_24(script)

	local HRP = game.Players.LocalPlayer.Character.HumanoidRootPart
	local char = game.Players.LocalPlayer.Character
	local Runservice = game:GetService("RunService")
	local stealflags = script.Parent.Parent
	stealflags.MouseButton1Click:Connect(function()
		local TeamName = string.split(tostring(game.Players.LocalPlayer.Team), " ")[1]:lower()
		for i, v in pairs(game.Workspace.Ignore.flags:GetChildren()) do
			repeat task.wait(0.2)
				HRP.CFrame = CFrame.new(v.Position + Vector3.new(0, 4, 0))
			until ((v.Position - HRP.Position).Magnitude < 10)
			task.wait(0.2)
			v.flag_interact:FireServer()
			task.wait(0.2)
			if char:FindFirstChild("flag") then
				repeat task.wait()
					HRP.CFrame = CFrame.new(game.Workspace.World["Flag Stands"][TeamName.." flags"].base.Position + Vector3.new(0, 3, 0))
				until not char:FindFirstChild("flag")
			end
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_25(script)

	local Players = game:GetService("Players")
	local player = Players.LocalPlayer
	local playerChar = player.Character or player.CharacterAdded:Wait()
	local shadow = script.Parent
	local btn = shadow.Parent


	btn.MouseButton1Click:Connect(function()
		for i,v in pairs(playerChar:GetChildren()) do
			if v:IsA("CharacterMesh") then
				v:Destroy()
			end
		end
	end)
end

local function LocalScript_22(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local TweenService = game:GetService("TweenService")
	local pressed = false

	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local CombatTweenClick = TweenService:Create(
		Combat,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(244,63,63)}
	)
	local ShadowTweenClick = TweenService:Create(
		Shadow,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(145,0,0)}

	)

	Combat.MouseButton1Click:Connect(function()
		if not pressed then
			pressed = true
			CombatTween:Play()
			ShadowTween:Play()
			wait(0.5)
			Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
			Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)
		else
			pressed = false
			ShadowTweenClick:Play()
			CombatTweenClick:Play()
			wait(0.5)
			Combat.BackgroundColor3 = Color3.fromRGB(244,63,63)
			Shadow.BackgroundColor3 = Color3.fromRGB(145,0,0)
		end
	end)
end

--------------------------------------------------
----------------------------------------------------------------------------------------------------

local function LocalScript_23(script)
	local EconomyTab = script.Parent
	local ReplicatedStorage = game:GetService("ReplicatedStorage")
	local selectedTree = EconomyTab.selectedTree
	local selectedOre = EconomyTab.selectedOre
	local TreesFolder = require(ReplicatedStorage.Trees)
	local OresFolder = require(ReplicatedStorage.Ores)
	local TreeTracker = game.Workspace["Tree Tracker"]
	local OreList = EconomyTab["Ore List"]
	local TreeList = EconomyTab["Tree List"]
	local AutoFarmOres = EconomyTab["Auto Farm Ore"]
	local AutoFarmTrees = EconomyTab["Auto Farm Tree"]
	local WhitelistOres = EconomyTab["Whitelist Ore"]
	local WhitelistTree = EconomyTab["Whitelist Tree"]
	local OreDummy = OreList["Ore Dummy"]
	local TreeDummy = TreeList["Tree Dummy"]
	local TreesClone = TreeDummy:Clone()
	local FrameClone = TreeDummy.Frame:Clone()
	local OreClone = OreDummy:Clone()
	local RunService = game:GetService("RunService")
	local ReplicatedStorage = game:GetService("ReplicatedStorage")
	local Communication = ReplicatedStorage:WaitForChild("Communication")
	local SellItem = Communication:FindFirstChild("SellTradeItem")
	local RequestOreCut_Regular = Communication:FindFirstChild("RequestOreCut_Regular")
	local mineval = EconomyTab.Parent["Off Tab"].Frame.MinedValue.mineval
	local chopval = EconomyTab.Parent["Off Tab"].Frame.ChoppedValue.chopval
	local Players = game:GetService("Players")
	local player = Players.LocalPlayer
	local UIS = game:GetService("UserInputService")
	local tree_Farm
	local Character = player.Character or player.CharacterAdded:Wait()
	local trees
	local ores
	local ore_Farm
	local treeFrame
	local pressed = false
	local pressed2 = false
	_G.Debounce = false
	table.insert(TreesFolder, "strong wood")
	table.insert(TreesFolder, "wood")


	TreesClone.Parent = TreeList
	FrameClone.Parent = TreesClone
	OreClone.Parent = OreList

	for i,tree in pairs(TreesFolder) do
		trees = TreesClone:Clone()
		for i,v in pairs(trees:GetChildren()) do
			if v:IsA("Frame") then
				v:Destroy()
			end
		end
		treeFrame = FrameClone:Clone()
		trees.Parent = TreeList
		treeFrame.Parent = trees
		trees.Name = tree
		trees.Text = tree
	end
	TreesClone:Destroy()
	FrameClone:Destroy()
	TreeDummy:Destroy()


	for i,txtbtn in pairs(TreeList:GetChildren()) do
		if txtbtn:IsA("TextButton") then
			txtbtn.MouseButton1Click:Connect(function()
				selectedTree.Name = txtbtn.Name
				selectedTree.Text = txtbtn.Name
			end)
		end
	end

	for i,ore in pairs(OresFolder) do
		ores = OreClone:Clone()
		ores.Parent = OreList
		ores.Name = ore
		ores.Text = ore
	end

	for i,txtbtn in pairs(OreList:GetChildren()) do
		if txtbtn:IsA("TextButton") then
			txtbtn.MouseButton1Click:Connect(function()
				selectedOre.Name = txtbtn.Name
				selectedOre.Text = txtbtn.Name
			end)
		end
	end



	OreClone:Destroy()
	OreDummy:Destroy()

	local Heartbeatloop
	local function auraOn()
		local Shadow = script.Parent
		local Combat = Shadow.Parent
		local TweenService = game:GetService("TweenService")
		local RunService = game:GetService("RunService")
		local Players = game:GetService("Players")
		local ReplicatedStorage = game:GetService("ReplicatedStorage")
		local Communication = ReplicatedStorage:WaitForChild("Communication")
		local HttpService = game:GetService("HttpService")
		local MeleeTalk = Communication:FindFirstChild("MeleeTalk")
		local bow_aura
		local kill_aura
		local pressed = false
		local oldpos
		local attach


		local function RegionCheck(HRP)
			for _,Region in pairs(ReplicatedStorage["world regions"]:GetChildren()) do
				if string.match(Region.Name,"graveyard") and table.find(workspace:FindPartsInRegion3(Region3.new(Region.Position - (0.5 * Region.Size),Region.Position + (0.5 * Region.Size)),nil,math.huge),HRP) then
					return false
				end
			end
			return true
		end

		local player = Players.LocalPlayer
		local function getClosestPlayer()
			local maximumDistance = 30;
			local playerFound
			for i,target in pairs(Players:GetPlayers()) do
				if (target.Team ~= player.Team and target.Name ~= player.Name and target.Team.Name ~= "Loading") then
					local targetChar = target.Character or target.CharacterAdded:Wait()
					local clientChar = player.Character or player.CharacterAdded:Wait()
					if (targetChar) then
						local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")
						local targetHumanoid = targetChar:FindFirstChild("Humanoid")
						local clientHRP = clientChar:FindFirstChild("HumanoidRootPart")
						local clientHumanoid = clientChar:FindFirstChild("Humanoid")
						if(targetHRP and clientHRP and targetHumanoid and clientHumanoid and targetHumanoid.Health > 0 and clientHumanoid.Health > 0) then
							local x,y,z = targetHRP.Position.X, targetHRP.Position.Y, targetHRP.Position.Z
							local PLAYER_DISTANCE = player:DistanceFromCharacter(clientHRP.Position)
							if (PLAYER_DISTANCE < maximumDistance) then
								if (x > -2000 and x < 2000) then
									if (y > 0 and y < 1000) then
										if (z > -2000 and z < 2000) then
											if (RegionCheck(targetHRP)) then
												maximumDistance = PLAYER_DISTANCE
												playerFound = targetChar
											end
										end
									end
								end
							end
						end
					end
				end
			end
			return playerFound
		end
		local function bowaura()
			for i,target in pairs(Players:GetPlayers()) do
				if (target.Team ~= player.Team and target.Name ~= player.Name and target.Team.Name ~= "Loading") then
					local targetChar = target.Character or target.CharacterAdded:Wait()
					local clientChar = player.Character or player.CharacterAdded:Wait()
					if (targetChar) then
						local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")
						local targetHumanoid = targetChar:FindFirstChild("Humanoid")
						local clientHRP = clientChar:FindFirstChild("HumanoidRootPart")
						local clientHumanoid = clientChar:FindFirstChild("Humanoid")
						if(targetHRP and clientHRP and targetHumanoid and clientHumanoid and targetHumanoid.Health > 0 and clientHumanoid.Health > 0) then
							local playerTarget = getClosestPlayer()
							if (playerTarget) then
								local x,y,z = targetHRP.Position.X, targetHRP.Position.Y, targetHRP.Position.Z
								if (x > -2000 and x < 2000) then
									if (y > 0 and y < 1000) then
										if (z > -2000 and z < 2000) then
											local playerCharacter = playerTarget
											local playerHRP = playerCharacter.HumanoidRootPart
											local playerPos = playerHRP.Position
											local args = {
												nil,
												tostring(playerTarget.Name),
												0,
												playerHRP.CFrame,
												playerPos,
												playerPos,
												'{"Z":-nan(ind),"X":-nan(ind),"Y":-nan(ind)}',
												nil,
												"faIse",
												false
											}
											Communication.RangedTalk:FireServer(unpack(args))
										end
									end
								end
							end
						end
					end
				end
			end
		end

		local function killaura()
			local targets = {}
			for i,target in pairs(Players:GetChildren()) do
				if (target.Name ~= player.Name and target.Team ~= player.Team and target.UserId ~= player.UserId and target.Team.Name ~= "Loading") then
					local targetCharacter = target.Character or target.CharacterAdded:Wait()
					if targetCharacter then
						local targetHumanoid = targetCharacter.Humanoid or targetCharacter:WaitForChild("Humanoid")
						local targetHumanoidRootPart = targetCharacter.HumanoidRootPart or targetCharacter:FindFirstChild("HumanoidRootPart")
						if (targetHumanoid and targetHumanoidRootPart and targetHumanoid.Health > 0) then
							local playerDistance = player:DistanceFromCharacter(targetHumanoidRootPart.Position)
							if (playerDistance <= 30) then
								if RegionCheck(targetHumanoid) then
									local userId = Players:GetUserIdFromNameAsync(target.Name)
									table.insert(targets, {
										UD = userId,
										distance = playerDistance
									})
								end
							end
						end
					end
				end
			end
			if (#targets > 0) then
				local USERIDS = {}

				table.sort(targets, function(a, b)
					return a.distance < b.distance
				end)

				for i = 1, math.min(#targets, 5) do
					table.insert(USERIDS, targets[i].UD);
				end
				MeleeTalk:FireServer(nil, HttpService:JSONEncode(USERIDS), "faIse", false)
			end
		end
		local loop = task.spawn(function()
			bowaura()
			killaura()	
		end)
	end




	WhitelistTree.MouseButton1Click:Connect(function()
		local TweenService = game:GetService("TweenService")
		local function tableFinder()
			if table.find(TreesFolder, selectedTree.Text) then
				return true
			elseif not table.find(TreesFolder, selectedTree.Text) then
				return false
			end
		end

		local tableResolver = tableFinder()

		if tableResolver then
			for i,btn in pairs(TreeList:GetChildren()) do
				if btn.Name == selectedTree.Text then
					local tableFind = table.find(TreesFolder, selectedTree.Text)
					table.remove(TreesFolder, tableFind)
					if btn then
						print(btn.Name)
						local Shadow = btn:FindFirstChildWhichIsA("Frame")
						local Combat = btn
						local ShadowTween = TweenService:Create(
							Shadow,
							TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
							{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
						)
						local CombatTween = TweenService:Create(
							Combat,
							TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
							{BackgroundColor3 = Color3.fromRGB(0, 160, 90)}
						)
						CombatTween:Play()
						ShadowTween:Play()					
					end
				end
			end
		elseif not tableResolver then
			for i,btn in pairs(TreeList:GetChildren()) do
				if btn:IsA("TextButton") then
					if btn.Name == selectedTree.Text then
						table.insert(TreesFolder, selectedTree.Text)
						for i,v in pairs(TreesFolder) do
							print("WHITELISTED TREES: "..v)
						end
						local Shadow = btn:FindFirstChildWhichIsA("Frame")
						local Combat = btn
						local ShadowTween = TweenService:Create(
							Shadow,
							TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
							{BackgroundColor3 = Color3.fromRGB(130, 130, 130)}
						)
						local CombatTween = TweenService:Create(
							Combat,
							TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
							{BackgroundColor3 = Color3.fromRGB(45, 45, 45)}
						)
						CombatTween:Play()
						ShadowTween:Play()	
					end
				end
			end
		end
	end)

	AutoFarmOres.MouseButton1Click:Connect(function()

		local Shadow = AutoFarmOres.Shadow
		local Combat = AutoFarmOres
		local TweenService = game:GetService("TweenService")

		local ShadowTween = TweenService:Create(
			Shadow,
			TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
			{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
		)


		local CombatTween = TweenService:Create(
			Combat,
			TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
			{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
		)

		local CombatTweenClick = TweenService:Create(
			Combat,
			TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
			{BackgroundColor3 = Color3.fromRGB(244,63,63)}
		)
		local ShadowTweenClick = TweenService:Create(
			Shadow,
			TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
			{BackgroundColor3 = Color3.fromRGB(145,0,0)}

		)

		local OresInstance = game.Workspace.Ores_Regular
		local RunService = game:GetService("RunService")
		local ReplicatedStorage = game:GetService("ReplicatedStorage")
		local Communication = ReplicatedStorage:WaitForChild("Communication")
		local Players = game:GetService("Players")
		local player = Players.LocalPlayer
		local Character = player.Character or player.CharacterAdded:Wait()
		local InventorySlots = ReplicatedStorage.Player_Data[player.UserId].Save_Data.inventory_slots.Value
		local HRP = Character:WaitForChild("HumanoidRootPart")
		HRP.CFrame = CFrame.new(-209, 298, -44)
		if not pressed2 then
			local Shadow = script.Parent
			local Combat = Shadow.Parent
			local TweenService = game:GetService("TweenService")
			local pressed = false
			local Players = game:GetService("Players")
			local player = Players.LocalPlayer
			local ReplicatedStorage = game:GetService("ReplicatedStorage")
			local Communication = ReplicatedStorage:WaitForChild("Communication")
			local RunService = game:GetService("RunService")
			local HttpService = game:GetService("HttpService")
			local MeleeTalk = Communication:WaitForChild("MeleeTalk")
			local MAXIMUM_DISTANCE = math.huge;
			local bow_aura = nil
			local closestChar = nil
			pressed2 = true
			CombatTween:Play()
			ShadowTween:Play()
			wait(0.5)
			Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
			Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)
			_G.Looping = true
			task.spawn(function()
				Heartbeatloop = RunService.Heartbeat:Connect(function()
					auraOn()
				end)
			end)
			local function automine(part)
				if part:IsA("Part") then
					print(part)
					local HumanoidRootPart = Character:WaitForChild("HumanoidRootPart")
					local fullPos = Vector3.new(part.Position.X + 0, part.Position.Y + 5, part.Position.Z + 0)
					HumanoidRootPart.CFrame = CFrame.new(fullPos)
					local InventoryHolder = game:GetService("HttpService"):JSONDecode(game:GetService("ReplicatedStorage").Player_Data[game.Players.LocalPlayer.UserId].Save_Data.materials.Value)
					if #InventoryHolder ~= InventorySlots and pressed2 == true then
						print(#InventoryHolder)
						repeat wait()
							local orePickedName = part.Parent.Name
							local args = 
								{
									function() end,
									tostring(orePickedName),
									"faIse",
									false
								}
							game:GetService("ReplicatedStorage").Communication.RequestOreCut_Regular:InvokeServer(unpack(args))
						until part.Parent == nil
						mineval.Value += 1 
					elseif #InventoryHolder == InventorySlots and pressed2 == true then
						print("Full ,,, "..#InventoryHolder)
						HumanoidRootPart.CFrame = CFrame.new(75, 382, -134)
						HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-45), 0)
						for i,v in pairs(OresFolder) do
							SellItem:InvokeServer(v, true)
						end
					end
				end
			end
			local function updateMiner()
				if OresInstance and pressed2 == true then
					for _, ores in pairs(OresInstance:GetChildren()) do
						if ores and Character and pressed2 == true then
							for _, pos in pairs(ores:GetChildren()) do
								_G.Looping = true
								automine(pos)
							end
						end
					end
				end
				_G.Looping = false
			end
			updateMiner()
			while _G.Looping == false do
				wait()
				updateMiner()
			end
		else
			Heartbeatloop:Disconnect()
			pressed2 = false
			ShadowTweenClick:Play()
			CombatTweenClick:Play()
			wait(0.25)
			Combat.BackgroundColor3 = Color3.fromRGB(244,63,63)
			Shadow.BackgroundColor3 = Color3.fromRGB(145,0,0)
			_G.Looping = true
		end

	end)


	WhitelistOres.MouseButton1Click:Connect(function()
		print("UHG")
		local TweenService = game:GetService("TweenService")
		local function tableFinder()
			if table.find(OresFolder, selectedOre.Text) then
				return true
			elseif not table.find(OresFolder, selectedOre.Text) then
				return false
			end
		end

		local tableResolver = tableFinder()

		if tableResolver then
			for i,btn in pairs(OreList:GetChildren()) do
				if btn.Name == selectedOre.Text then
					local tableFind = table.find(OresFolder, selectedOre.Text)
					table.remove(OresFolder, tableFind)
					if btn then
						print(btn.Name)
						local Shadow = btn:FindFirstChildWhichIsA("Frame")
						local Combat = btn
						local ShadowTween = TweenService:Create(
							Shadow,
							TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
							{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
						)
						local CombatTween = TweenService:Create(
							Combat,
							TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
							{BackgroundColor3 = Color3.fromRGB(0, 160, 90)}
						)
						CombatTween:Play()
						ShadowTween:Play()					
					end
				end
			end
		elseif not tableResolver then
			for i,btn in pairs(OreList:GetChildren()) do
				if btn:IsA("TextButton") then
					if btn.Name == selectedOre.Text then
						table.insert(OresFolder, selectedOre.Text)
						for i,v in pairs(OresFolder) do
							print("WHITELISTED TREES: "..v)
						end
						local Shadow = btn:FindFirstChildWhichIsA("Frame")
						local Combat = btn
						local ShadowTween = TweenService:Create(
							Shadow,
							TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
							{BackgroundColor3 = Color3.fromRGB(130, 130, 130)}
						)
						local CombatTween = TweenService:Create(
							Combat,
							TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
							{BackgroundColor3 = Color3.fromRGB(45, 45, 45)}
						)
						CombatTween:Play()
						ShadowTween:Play()	
					end
				end
			end
		else
			print('rrrrrr')
		end
	end)

	AutoFarmTrees.MouseButton1Click:Connect(function()

		local Shadow = AutoFarmTrees.Shadow
		local Combat = AutoFarmTrees
		local TweenService = game:GetService("TweenService")

		local ShadowTween = TweenService:Create(
			Shadow,
			TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
			{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
		)


		local CombatTween = TweenService:Create(
			Combat,
			TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
			{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
		)

		local CombatTweenClick = TweenService:Create(
			Combat,
			TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
			{BackgroundColor3 = Color3.fromRGB(244,63,63)}
		)
		local ShadowTweenClick = TweenService:Create(
			Shadow,
			TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
			{BackgroundColor3 = Color3.fromRGB(145,0,0)}

		)
		if not pressed then
			pressed = true
			CombatTween:Play()
			ShadowTween:Play()
			wait(0.5)
			Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
			Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)
			local RunService = game:GetService("RunService")
			local ReplicatedStorage = game:GetService("ReplicatedStorage")
			local TreesFolderr = game.Workspace.trees
			local Communication = ReplicatedStorage:WaitForChild("Communication")
			local Players = game:GetService("Players")
			local player = Players.LocalPlayer
			local UIS = game:GetService("UserInputService")
			local Character = player.Character or player.CharacterAdded:Wait()
			local InventorySlots = ReplicatedStorage.Player_Data[player.UserId].Save_Data.inventory_slots.Value
			local InventorySpace = Players.LocalPlayer.PlayerGui.main["main guis"].inventory.holder.left.top.space.txt
			local inventory = Players.LocalPlayer.PlayerGui.main["main guis"].inventory
			local MainGuiHandle = game:GetService("Players").LocalPlayer.PlayerGui.main["main guis"].MainGuiHandle
			local holder = game:GetService("Players").LocalPlayer.PlayerGui.main["main guis"].inventory.holder
			local title = inventory.title
			local line = inventory.line
			_G.FarmingTree = true
			for i,tree in pairs(TreesFolderr:GetChildren()) do
				if tree then
					for _,xcall in pairs(tree:GetChildren()) do
						if xcall.Name == "Tree" then
							xcall.CanCollide = false
						end
					end
				end
			end
			task.spawn(function()
				Heartbeatloop = RunService.Heartbeat:Connect(function()
					auraOn()
				end)
			end)
			local function AutoChop(part)
				if part:IsA("Part") and part.Name == "base" then
					local HumanoidRootPart = Character:WaitForChild("HumanoidRootPart")
					local fullPos = Vector3.new(part.Position.X + 0, part.Position.Y + 5, part.Position.Z + 0)
					HumanoidRootPart.CFrame = CFrame.new(fullPos)
					local InventoryHolder = game:GetService("HttpService"):JSONDecode(ReplicatedStorage.Player_Data[game.Players.LocalPlayer.UserId].Save_Data.materials.Value)
					if #InventoryHolder ~= InventorySlots  and pressed == true then
						print("True")
						print(#InventoryHolder)
						repeat wait()
							if part.Parent ~= nil then
								local treeName = part.Parent.Name
								Communication:WaitForChild("RequestTreeCut"):InvokeServer(nil, treeName, "faIse", false)
							end
						until part.Parent == nil
						chopval.Value += 1
					elseif #InventoryHolder == InventorySlots and pressed == true then
						print("False")
						HumanoidRootPart.CFrame = CFrame.new(75, 382, -134)
						HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-45), 0)
						for i,v in pairs(TreesFolder) do
							SellItem:InvokeServer(v, true)
						end
					end
				end
			end
			local function updateTree()
				if TreesFolderr and pressed == true then
					for i,v in ipairs(TreeTracker:GetChildren()) do
						if table.find(TreesFolder, v.Name) then
							_G.FarmingTree = true
							local WoodBase = v.Adornee
							AutoChop(WoodBase)
						end
					end
				end
				_G.FarmingTree = false
			end
			updateTree()
			while _G.FarmingTree == false do
				wait()
				updateTree()
			end
		else
			Heartbeatloop:Disconnect()
			pressed = false
			ShadowTweenClick:Play()
			CombatTweenClick:Play()
			wait(0.25)
			Combat.BackgroundColor3 = Color3.fromRGB(244,63,63)
			Shadow.BackgroundColor3 = Color3.fromRGB(145,0,0)
			_G.FarmingTree = true
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_35(script)

	local whitelist = {"Iuvtrenton"}
	local oldCFrame
	local function protect(plrName, Character, Hum, HumanoidRootPart)	
		local Character, Humanoid, HumanoidRootPart
		local Players = game:GetService("Players")
		local client = Players.LocalPlayer
		local function NewCharacter(Char)
			Character = Char
			if Char then
				HumanoidRootPart = Char:WaitForChild("HumanoidRootPart")
				Humanoid = Char:WaitForChild("Humanoid")

				local Player = Players[plrName]
				local RunService = game:GetService("RunService")
				local attach
				local clientChar
				local clientHumanoid
				local clientHRP
				local Teleported = false
				local attach
				local clientFinder
				local Teleported = false
				local playerChar
				local playerHumanoid
				local playerHRP
				local player = Players.LocalPlayer
				local ReplicatedStorage = game:GetService("ReplicatedStorage")
				local Communication = ReplicatedStorage:WaitForChild("Communication")
				local RunService = game:GetService("RunService")
				local MAXIMUM_DISTANCE = math.huge;
				local bow_aura = nil
				local closestChar = nil
				local distance = 30
				local player = Players.LocalPlayer
				local character = player.Character or player.CharacterAdded:Wait()
				local Humanoid = character.Humanoid or character:FindFirstChild("Humanoid")
				local LOCAL_ROOT_PART = character:FindFirstChild("HumanoidRootPart") or character.HumanoidRootPart
				local MeleeTalk = Communication:WaitForChild("MeleeTalk")
				local RunService = game:GetService("RunService")
				local HttpService = game:GetService("HttpService")
				local MAXIMUM_DISTANCE = math.huge;
				local bow_aura
				local kill_aura
				local CLOSEST_CHARACTER

				local function RegionCheck(HRP)
					for _,Region in pairs(ReplicatedStorage["world regions"]:GetChildren()) do
						if string.match(Region.Name,"graveyard") and table.find(workspace:FindPartsInRegion3(Region3.new(Region.Position - (0.5 * Region.Size),Region.Position + (0.5 * Region.Size)),nil,math.huge),HRP) then
							return false
						end
					end
					return true
				end
				local function CLIENTEXIST()
					local CLIENTCHARACTERHOLDER
					local CLIENTHUMANOIDHOLDER
					local CLIENTHUMANOIDROOTPARTHOLDER
					if client then
						CLIENTCHARACTERHOLDER = client.Character or client.CharacterAdded:Wait()
						if CLIENTCHARACTERHOLDER then
							CLIENTHUMANOIDHOLDER = CLIENTCHARACTERHOLDER:FindFirstChild("Humanoid") or CLIENTCHARACTERHOLDER:WaitForChild("Humanoid")
							CLIENTHUMANOIDROOTPARTHOLDER = CLIENTCHARACTERHOLDER:FindFirstChild("HumanoidRootPart") or CLIENTCHARACTERHOLDER:WaitForChild("HumanoidRootPart")
							if CLIENTHUMANOIDHOLDER.Parent == nil then return end
							if CLIENTHUMANOIDROOTPARTHOLDER and CLIENTHUMANOIDHOLDER then
								if CLIENTHUMANOIDHOLDER.Health > 0 then
									clientChar = CLIENTCHARACTERHOLDER
									clientHumanoid = CLIENTHUMANOIDHOLDER
									clientHRP = CLIENTHUMANOIDROOTPARTHOLDER
								end
							end
						end
					end
					return true
				end

				clientFinder = CLIENTEXIST()

				if Hum then
					local Tags = Hum:FindFirstChild("Tags")
					local LastTag = Hum:FindFirstChild("LastTag")
					LastTag.Changed:Connect(function()
						wait()
						for i,v in pairs(Tags:GetChildren()) do
							for i,c in pairs(v:GetChildren()) do
								print("C value = "..math.floor(c.Value))
								print("LastTag value = "..math.floor(LastTag.Value))
								if (math.floor(c.Value) == math.floor(LastTag.Value)) then
									table.insert(_G.Attacker, tonumber(c.Parent.Name))
									local enemy = Players:GetPlayerByUserId(tonumber(c.Parent.Name))
									local enemyCharacter = enemy.Character
									local enemyHumanoid = enemyCharacter:FindFirstChild("Humanoid")
									local enemyHRP = enemyCharacter:FindFirstChild("HumanoidRootPart")
									print(enemy.Name)
									repeat wait()
										local function attach()
											for _,player in pairs(Players:GetPlayers()) do
												if (player.Team ~= client.Team and client.Name ~= Player.Name and player.Name ~= "rawnull" and client.Name ~= "rawnull" and player.Team.Name ~= "Loading" and not table.find(_G.Protectee,player.UserId) and table.find(_G.Attacker, player.UserId)) then
													playerChar = player.Character or player.CharacterAdded:Wait()
													if playerChar then

														playerHumanoid = playerChar:FindFirstChild("Humanoid") or playerChar:WaitForChild("Humanoid")

														playerHRP = playerChar:FindFirstChild("HumanoidRootPart") or playerChar:WaitForChild("HumanoidRootPart")

														if (playerHumanoid and playerHRP and clientFinder and playerHumanoid.Health > 0) then

															local playerDistanceOutpost = player:DistanceFromCharacter(Vector3.new(613, 315, -90))

															if (playerDistanceOutpost < 2500) then
																if (RegionCheck(playerHRP)) then
																	local x,y,z = playerHRP.Position.X, playerHRP.Position.Y, playerHRP.Position.Z
																	if (x > -2000 and x < 2000) then
																		if (y > -1000 and y < 1000) then
																			if (z > -2000 and z < 2000) then
																				clientHRP.CFrame = playerHRP.CFrame
																			end
																		end
																	end
																end
															else
																clientHRP.CFrame = CFrame.new(617, 323, 1143)
															end

														end

													end

												end

											end

										end


										local function getClosestPlayer()
											local maximumDistance = 3000;
											local playerFound
											for i,target in pairs(Players:GetPlayers()) do
												if (target.Team ~= player.Team  and player.Name ~= Player.Name and target.Name ~= "Iuvtrenton" and target.Team.Name ~= "Loading" and not table.find(_G.Protectee,target.UserId) and table.find(_G.Attacker, target.UserId)) then
													local targetChar = target.Character or target.CharacterAdded:Wait()
													local clientChar = player.Character or player.CharacterAdded:Wait()
													if (targetChar) then
														local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")
														local targetHumanoid = targetChar:FindFirstChild("Humanoid")
														local clientHRP = clientChar:FindFirstChild("HumanoidRootPart")
														local clientHumanoid = clientChar:FindFirstChild("Humanoid")
														if(targetHRP and clientHRP and targetHumanoid and clientHumanoid and targetHumanoid.Health > 0 and clientHumanoid.Health > 0) then
															local x,y,z = targetHRP.Position.X, targetHRP.Position.Y, targetHRP.Position.Z
															local PLAYER_DISTANCE = target:DistanceFromCharacter(Vector3.new(609, 315, -92))
															if (PLAYER_DISTANCE < maximumDistance) then
																if (x > -2000 and x < 2000) then
																	if (y > -1000 and y < 1000) then
																		if (z > -2000 and z < 2000) then
																			maximumDistance = PLAYER_DISTANCE
																			playerFound = targetChar
																		end
																	end
																end
															end
														end
													end
												end
											end
											return playerFound
										end


										local function bowaura()
											for i,target in pairs(Players:GetPlayers()) do
												if (target.Team ~= player.Team and player.Name ~= Player.Name and target.Name ~= player.Name and target.Name ~= "rawnull" and player.Name ~= "rawnull" and target.Team.Name ~= "Loading" and not table.find(_G.Protectee,target.UserId) and table.find(_G.Attacker, target.UserId)) then
													local targetChar = target.Character or target.CharacterAdded:Wait()
													local clientChar = player.Character or player.CharacterAdded:Wait()
													if (targetChar) then
														local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")
														local targetHumanoid = targetChar:FindFirstChild("Humanoid")
														local clientHRP = clientChar:FindFirstChild("HumanoidRootPart")
														local clientHumanoid = clientChar:FindFirstChild("Humanoid")
														if(targetHRP and clientHRP and targetHumanoid and clientHumanoid and targetHumanoid.Health > 0 and clientHumanoid.Health > 0) then
															local playerTarget = getClosestPlayer()
															if (playerTarget) then
																local xz,yz,zz = targetHRP.Position.X, targetHRP.Position.Y, targetHRP.Position.Z
																if (xz > -2000 and xz < 2000) then
																	if (yz > -1000 and yz < 1000) then
																		if (zz > -2000 and zz < 2000) then
																			local playerCharacter = playerTarget
																			local playerHRP = playerCharacter.HumanoidRootPart
																			local playerPos = playerHRP.Position
																			local args = {
																				nil,
																				tostring(playerTarget.Name),
																				0,
																				playerHRP.CFrame,
																				playerPos,
																				playerPos,
																				'{"Z":-nan(ind),"X":-nan(ind),"Y":-nan(ind)}',
																				nil,
																				"faIse",
																				false
																			}
																			Communication.RangedTalk:FireServer(unpack(args))
																		end
																	end
																end
															end
														end
													end
												end
											end
										end



										local function killaura()
											local targets = {}
											for i,target in pairs(Players:GetChildren()) do
												if (target.Name ~= player.Name and target.Team ~= player.Team and target.UserId ~= player.UserId and target.Team.Name ~= "Loading" and target.Name ~= "rawnull" and player.Name ~= "rawnull" and not table.find(_G.Protectee,target.UserId) and table.find(_G.Attacker, target.UserId)) then
													local targetCharacter = target.Character or target.CharacterAdded:Wait()
													if targetCharacter then
														local targetHumanoid = targetCharacter.Humanoid or targetCharacter:WaitForChild("Humanoid")
														local targetHumanoidRootPart = targetCharacter.HumanoidRootPart or targetCharacter:FindFirstChild("HumanoidRootPart")
														if (targetHumanoid and targetHumanoidRootPart and targetHumanoid.Health > 0) then
															local playerDistance = player:DistanceFromCharacter(targetHumanoidRootPart.Position)
															if (playerDistance <= 30) then
																local userId = Players:GetUserIdFromNameAsync(target.Name)
																table.insert(targets, {
																	UD = userId,
																	distance = playerDistance
																})
															end
														end
													end
												end
											end
											if (#targets > 0) then
												local USERIDS = {}

												table.sort(targets, function(a, b)
													return a.distance < b.distance
												end)

												for i = 1, math.min(#targets, 5) do
													table.insert(USERIDS, targets[i].UD);
												end
												MeleeTalk:FireServer(nil, HttpService:JSONEncode(USERIDS), "faIse", false)
											end
										end
										task.spawn(function()
											attach()
											bowaura()
											killaura()
										end)
									until enemyHumanoid.Health == 0
									if client.Name ~= "Iuvtrenton" then 
										clientHRP.CFrame = CFrame.new(617, 323, 1143)
									end


									_G.Attacker = {}
								end
							end
						end
					end)
				end

				_G.Attacker = {}
			else
				Humanoid, HumanoidRootPart = nil
			end
		end

		NewCharacter(client.Character)
		client.CharacterAdded:Connect(NewCharacter)
	end


	for i,plr in pairs(game:GetService("Players"):GetPlayers()) do
		if table.find(whitelist, plr.Name) then
			local Character, Humanoid, HumanoidRootPart
			local function NewCharacter(Char) -- Player.CharacterAdded
				Character = Char
				if Char then
					HumanoidRootPart = Char:WaitForChild("HumanoidRootPart")
					Humanoid = Char:WaitForChild("Humanoid")
					protect(plr.Name, Character, Humanoid, HumanoidRootPart)
				else
					Humanoid, HumanoidRootPart = nil
				end
			end

			NewCharacter(plr.Character)
			plr.CharacterAdded:Connect(NewCharacter)
		else
			game.Players.PlayerAdded:Connect(function(plr)
				if plr.Name ~= "Iuvtrenton" and not game:GetService("Players"):FindFirstChild("Iuvtrenton") then return end
				if plr.Name == "Iuvtrenton" and game:GetService("Players"):FindFirstChild("Iuvtrenton") then
					if plr.Team.Name ~= "Loading" then
						local Character, Humanoid, HumanoidRootPart

						local function NewCharacter(Char) -- Player.CharacterAdded
							Character = Char
							if Char then
								HumanoidRootPart = Char:WaitForChild("HumanoidRootPart")
								Humanoid = Char:WaitForChild("Humanoid")
								protect(plr.Name, Character, Humanoid, HumanoidRootPart)
							else
								Humanoid, HumanoidRootPart = nil
							end
						end

						NewCharacter(plr.Character)
						plr.CharacterAdded:Connect(NewCharacter)
					end
				end
			end)
		end
	end
end

----------------------------------------------------------------------------------------------------

local function LocalScript_36(script)

	local Players = game:GetService("Players")
	local user = Players.LocalPlayer
	local Bots = {"CinovrasBot1", "CinovrasBot1", "CinovrasBot1", "CinovrasBot1"}
	local ChatEvent = game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents
	local messenger = "Iuvtrenton"

	local called = false


	ChatEvent.OnMessageDoneFiltering.OnClientEvent:Connect(function(msg)
		if msg.FromSpeaker == messenger then
			local trinitySinityPlayer = Players.rawnull
			local trinitySinityChar = trinitySinityPlayer.Character or trinitySinityPlayer.CharacterAdded:Wait()
			local trinitySinity = trinitySinityChar.HumanoidRootPart
			if msg.Message == ";def blue" then
				if user.Name == Bots[1] then
					user.Character.HumanoidRootPart.CFrame = CFrame.new(677, 315, -85)
					game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-45), 0)
					game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-45), 0)
				elseif user.Name == Bots[2] then
					user.Character.HumanoidRootPart.CFrame = CFrame.new(677, 315, -90)
					game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-45), 0)
					game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-45), 0)

				elseif user.Name == Bots[3] then
					user.Character.HumanoidRootPart.CFrame = CFrame.new(677, 315, -95)
					game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-45), 0)
					game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-45), 0)
				end
			elseif msg.Message == ";def red" then
				if user.Name == Bots[1] then
					user.Character.HumanoidRootPart.CFrame = CFrame.new(605, 315, -9)
					game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-45), 0)
					game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-45), 0)
				elseif user.Name == Bots[2] then
					user.Character.HumanoidRootPart.CFrame = CFrame.new(610, 315, -9)
					game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-45), 0)
					game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-45), 0)
				elseif user.Name == Bots[3] then
					user.Character.HumanoidRootPart.CFrame = CFrame.new(615, 315, -9)
					game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-45), 0)
					game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-45), 0)
				end
			elseif msg.Message == ";def grey" then
				if user.Name == Bots[1] then
					user.Character.HumanoidRootPart.CFrame = CFrame.new(545, 315, -85)
					game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-180), 0)
					game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-180), 0)
				elseif user.Name == Bots[2] then
					user.Character.HumanoidRootPart.CFrame = CFrame.new(545, 315, -90)
					game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-180), 0)
					game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-180), 0)
				elseif user.Name == Bots[3] then
					user.Character.HumanoidRootPart.CFrame = CFrame.new(545, 315, -95)
					game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-180), 0)
					game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(-180), 0)
				end
			elseif msg.Message == ";def green" then
				if user.Name == Bots[1] then
					user.Character.HumanoidRootPart.CFrame = CFrame.new(605, 315, -173)
				elseif user.Name == Bots[2] then
					user.Character.HumanoidRootPart.CFrame = CFrame.new(610, 315, -173)
				elseif user.Name == Bots[3] then
					user.Character.HumanoidRootPart.CFrame = CFrame.new(615, 315, -173)
				end
			elseif msg.Message == ";gather" then
				if table.find(Bots, user.Name) then
					called = true
					user.Character.HumanoidRootPart.CFrame = CFrame.new(605, 315, -94)
				end
			elseif msg.Message == ";return" then
				if table.find(Bots, user.Name) then
					called = true
					user.Character.HumanoidRootPart.CFrame = CFrame.new(617, 323, 1143)
				end
			elseif msg.Message == ";wings" then
				called = false
				repeat wait()
					if(trinitySinityChar and trinitySinity) then
						if user.Name == Bots[1] then
							user.Character.HumanoidRootPart.CFrame = trinitySinity.CFrame.Rotation + (trinitySinity.CFrame * Vector3.new(3, 0, 3))
						end
						if user.Name == Bots[2] then
							user.Character.HumanoidRootPart.CFrame = trinitySinity.CFrame.Rotation + (trinitySinity.CFrame * Vector3.new(-3, 0, 3))
						end
						if user.Name == Bots[3] then
							user.Character.HumanoidRootPart.CFrame = trinitySinity.CFrame.Rotation + (trinitySinity.CFrame * Vector3.new(-6, 0, 6))
						end
						if user.Name == Bots[4] then
							user.Character.HumanoidRootPart.CFrame = trinitySinity.CFrame.Rotation + (trinitySinity.CFrame * Vector3.new(6, 0, 6))
						end
					end
				until called == true
				user.Character.HumanoidRootPart.CFrame = CFrame.new(-165, 413, 168)
			elseif msg.Message == ";straight" then
				called = false
				repeat wait()
					if (trinitySinityChar and trinitySinity) then
						if user.Name == Bots[1] then
							user.Character.HumanoidRootPart.CFrame = trinitySinity.CFrame.Rotation + (trinitySinity.CFrame * Vector3.new(3, 0, 0))
						end
						if user.Name == Bots[2] then
							user.Character.HumanoidRootPart.CFrame = trinitySinity.CFrame.Rotation + (trinitySinity.CFrame * Vector3.new(-3, 0, 0))
						end
						if user.Name == Bots[3] then
							user.Character.HumanoidRootPart.CFrame = trinitySinity.CFrame.Rotation + (trinitySinity.CFrame * Vector3.new(-6, 0, 0))
						end
						if user.Name == Bots[4] then
							user.Character.HumanoidRootPart.CFrame = trinitySinity.CFrame.Rotation + (trinitySinity.CFrame * Vector3.new(6, 0, 0))
						end
					end
				until called == true
				user.Character.HumanoidRootPart.CFrame = CFrame.new(-165, 413, 168)
			end
		end
	end)

end

----------------------------------------------------------------------------------------------------

local function LocalScript_34(script)


	_G.Attacker = {}
	_G.SellOres = {}
	_G.SellWoods = {}
	_G.SavedItems = {}
	_G.Ostracize = {}
	_G.Protectee = {3761075662, 3761056494, 3351957899, 3764345965, 2495392350, 2952739329, 3761081217, 3764343744, 3761078371, 3761072073, 3761085202}
	_G.PreviousAttack = {}



end

----------------------------------------------------------------------------------------------------

local function LocalScript_40(script)


end

----------------------------------------------------------------------------------------------------

local function LocalScript_21(script)

	local UserInputService = game:GetService("UserInputService")
	local Players = game:GetService("Players")
	local runService = (game:GetService("RunService"));
	local UserInputService = game:GetService("UserInputService")
	local TweenService = game:GetService("TweenService")
	local gui = script.Parent
	local debounce = false
	local instance = {
		["Bots_Tab"] = gui["Bots Tab"],
		["Misc_Tab"] = gui["Misc Tab"],
		["Button_Tab"] = gui["Button Tab"],
		["Combat_Tab"] = gui["Combat Tab"],
		["Economy_Tab"] = gui["Economy Tab"],
		["Players_Tab"] = gui["Players Tab"],
		["Search_Tab"] = gui["Search Tab"],
		["Title_Tab"] = gui["Title Tab"],
		["Off_Tab"] = gui["Off Tab"]

	}

	local mainTabs = {
		["Bots_Tab"] = instance.Bots_Tab,
		["Misc_Tab"] = instance.Misc_Tab,
		["Combat_Tab"] = instance.Combat_Tab,
		["Economy_Tab"] = instance.Economy_Tab,
		["Off_Tab"] = instance.Off_Tab
	}
	gui.Visible = false
	UserInputService.InputBegan:Connect(function(key, Processed)
		if Processed then return end
		if key.UserInputType == Enum.UserInputType.Keyboard then
			if key.UserInputState == Enum.UserInputState.Begin then
				if key.KeyCode == Enum.KeyCode.R then
					if not debounce then
						debounce = true
						gui.Visible = true
					else
						debounce = false
						gui.Visible = false
					end
				end
			end
		end
	end)
	debounce = false

	local button_Tab = gui["Button Tab"]
	button_Tab.Combat.MouseButton1Click:Connect(function()
		for i,v in pairs(mainTabs) do
			if v.Name == mainTabs.Combat_Tab.Name then
				mainTabs.Off_Tab.Visible = false
				v.Visible = true
			elseif v.Name ~= mainTabs.Combat_Tab.Name and mainTabs then
				mainTabs.Off_Tab.Visible = false
				v.Visible = false
			else
				v.Visible = false
			end
		end
	end)
	button_Tab.Misc.MouseButton1Click:Connect(function()
		for i,v in pairs(mainTabs) do
			if v.Name == mainTabs.Misc_Tab.Name then
				mainTabs.Off_Tab.Visible = false
				v.Visible = true
			elseif v.Name ~= mainTabs.Misc_Tab.Name and mainTabs then
				mainTabs.Off_Tab.Visible = false
				v.Visible = false
			else
				v.Visible = false
			end
		end
	end)

	button_Tab.Economy.MouseButton1Click:Connect(function()
		for i,v in pairs(mainTabs) do
			if v.Name == mainTabs.Economy_Tab.Name then
				mainTabs.Off_Tab.Visible = false
				v.Visible = true
			elseif v.Name ~= mainTabs.Economy_Tab.Name and mainTabs then
				mainTabs.Off_Tab.Visible = false
				v.Visible = false
			else
				v.Visible = false
			end
		end
	end)

	button_Tab.Bots.MouseButton1Click:Connect(function()
		for i,v in pairs(mainTabs) do
			if v.Name == mainTabs.Bots_Tab.Name then
				v.Visible = true
			elseif v.Name ~= mainTabs.Bots_Tab.Name and mainTabs then
				mainTabs.Off_Tab.Visible = false
				v.Visible = false
			else
				v.Visible = false
			end
		end
	end)
	button_Tab.Home.MouseButton1Click:Connect(function()
		for i,v in pairs(mainTabs) do
			if v.Name == mainTabs.Off_Tab.Name then
				v.Visible = true
			elseif v.Name ~= mainTabs.Off_Tab.Name and mainTabs then
				v.Visible = false
			else
				v.Visible = false
			end
		end
	end)


	local dragging
	local dragInput
	local dragStart
	local startPos

	function Lerp(a, b, m)
		return a + (b - a) * m
	end;

	local lastMousePos
	local lastGoalPos
	local DRAG_SPEED = (8); -- // The speed of the UI darg.
	function Update(dt)
		if not (startPos) then return end;
		if not (dragging) and (lastGoalPos) then
			gui.Position = UDim2.new(startPos.X.Scale, Lerp(gui.Position.X.Offset, lastGoalPos.X.Offset, dt * DRAG_SPEED), startPos.Y.Scale, Lerp(gui.Position.Y.Offset, lastGoalPos.Y.Offset, dt * DRAG_SPEED))
			return 
		end;

		local delta = (lastMousePos - UserInputService:GetMouseLocation())
		local xGoal = (startPos.X.Offset - delta.X);
		local yGoal = (startPos.Y.Offset - delta.Y);
		lastGoalPos = UDim2.new(startPos.X.Scale, xGoal, startPos.Y.Scale, yGoal)
		gui.Position = UDim2.new(startPos.X.Scale, Lerp(gui.Position.X.Offset, xGoal, dt * DRAG_SPEED), startPos.Y.Scale, Lerp(gui.Position.Y.Offset, yGoal, dt * DRAG_SPEED))
	end;

	gui.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = gui.Position
			lastMousePos = UserInputService:GetMouseLocation()

			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then
					dragging = false
				end
			end)
		end
	end)

	gui.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			dragInput = input
		end
	end)

	runService.Heartbeat:Connect(Update)

end

----------------------------------------------------------------------------------------------------

local function LocalScript_19(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local TweenService = game:GetService("TweenService")
	local Players = game:GetService("Players")
	local playerCombat = Combat.Parent
	local playersTab = playerCombat.Parent
	local playerList = playersTab["Player List"]
	local playerName = playerCombat.playerName
	local pressed = false
	local targetSelected
	local button
	local shadowbtn


	Combat.MouseButton1Click:Connect(function()
		targetSelected = playerName
		for i,txtbutton in pairs(playerList:GetChildren()) do
			if txtbutton:IsA("TextButton") and txtbutton.Name == targetSelected.Name then
				local targetselected = Players:GetUserIdFromNameAsync(targetSelected.Name)
				print("Found...")
				for i,v in pairs(txtbutton:GetChildren()) do
					if v:IsA("Frame") and v:FindFirstChild("Frame") then 
						print("Frame Found")
					end
					shadowbtn = v
					print("Assigned Shadowbtn Ostracize")
				end
				button = txtbutton
				print("Assigned Shadowbtn Ostracize")
			end
		end

		local ShadowTween = TweenService:Create(
			shadowbtn,
			TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
			{BackgroundColor3 = Color3.fromRGB(145, 0, 0)}
		)


		local CombatTween = TweenService:Create(
			button,
			TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
			{BackgroundColor3 = Color3.fromRGB(245, 63, 63)}
		)

		local ShadowTweenLeave = TweenService:Create(
			shadowbtn,
			TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
			{BackgroundColor3 = Color3.fromRGB(20, 20, 20)}
		)

		local CombatTweenLeave = TweenService:Create(
			button,
			TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
			{BackgroundColor3 = Color3.fromRGB(60,60,60)}
		)
		local targetselected = Players:GetUserIdFromNameAsync(targetSelected.Name)
		print(targetselected)
		if (not table.find(_G.Ostracize, targetselected)) then
			table.insert(_G.Ostracize,1,targetselected)
			CombatTween:Play()
			ShadowTween:Play()
			warn("ADDED [ ".. tostring(targetselected) .. " ] TO Ostracize")

		elseif table.find(_G.Ostracize, targetselected) then
			for i , protecteeRemove in pairs(_G.Ostracize) do
				local tableFinder = table.find(_G.Ostracize, targetselected)
				if tableFinder then
					table.remove(_G.Ostracize, tableFinder)
					warn("Removed ".. tostring(targetselected).. " From Ostracize")
					CombatTweenLeave:Play()
					ShadowTweenLeave:Play()
					for i,v in pairs(_G.Ostracize) do
						print("Key: "..i.. "-------".. "Value: ".. v)
					end
				end
			end
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_6(script)

	local TextBox = script.Parent
	local SearchTab = TextBox.Parent
	local playersTab = SearchTab.Parent["Players Tab"]
	local playerList = playersTab["Player List"]

	local function focusLost()
		wait(0.5)
		TextBox.Text = "Search..."
		for i,v in pairs(playerList:GetChildren()) do
			if v:IsA("TextButton") then
				v.Visible = true
			end
		end
	end

	local function update()
		TextBox:GetPropertyChangedSignal("Text"):Connect(function()
			local input = string.lower(TextBox.Text)
			for i,v in pairs(playerList:GetChildren()) do
				if v:IsA("TextButton") then
					if string.find(string.lower(v.Name), input) then
						v.Visible = true
					elseif not string.find(string.lower(v.Name), input) then
						v.Visible = false
					end
				end
			end
		end)
		for i,v in pairs(playerList:GetChildren()) do
			if v:IsA("TextButton") then
				v.Visible = true
			end
		end
	end
	update()




	TextBox.FocusLost:Connect(focusLost)

	TextBox.Focused:Connect(update)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_7(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local TweenService = game:GetService("TweenService")
	local pressed = false
	local Players = game.Players or game:GetService("Players")
	local player = Players.LocalPlayer
	local character = player.Character or player.CharacterAdded:Wait()
	local Humanoid = character.Humanoid or character:FindFirstChild("Humanoid")
	local LOCAL_ROOT_PART = character:FindFirstChild("HumanoidRootPart") or character.HumanoidRootPart
	local ReplicatedStorage = game:GetService("ReplicatedStorage")
	local Communication = ReplicatedStorage:WaitForChild("Communication")
	local MeleeTalk = Communication:WaitForChild("MeleeTalk")
	local RunService = game:GetService("RunService")
	local HttpService = game:GetService("HttpService")
	local MAXIMUM_DISTANCE = math.huge;
	local bow_aura
	local kill_aura
	local CLOSEST_CHARACTER

	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local CombatTweenClick = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(244,63,63)}
	)
	local ShadowTweenClick = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(145,0,0)}

	)

	Combat.MouseButton1Click:Connect(function()
		local distance = 30
		local UserId
		if not pressed then
			pressed = true
			CombatTween:Play()
			ShadowTween:Play()
			wait(0.25)
			Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
			Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)

			kill_aura = RunService.Heartbeat:Connect(function()
				local targets = {}
				for i,target in pairs(Players:GetChildren()) do
					if (target.Name ~= player.Name and target.Team ~= player.Team and target.UserId ~= player.UserId and not table.find(_G.Protectee,target.UserId)) then
						local targetCharacter = target.Character or target.CharacterAdded:Wait()
						if targetCharacter then
							local targetHumanoid = targetCharacter.Humanoid or targetCharacter:WaitForChild("Humanoid")
							local targetHumanoidRootPart = targetCharacter.HumanoidRootPart or targetCharacter:FindFirstChild("HumanoidRootPart")
							if (targetHumanoid and targetHumanoidRootPart and targetHumanoid.Health > 0) then
								local playerDistance = player:DistanceFromCharacter(targetHumanoidRootPart.Position)
								if (playerDistance <= 30) then
									local userId = Players:GetUserIdFromNameAsync(target.Name)
									table.insert(targets, {
										UD = userId,
										distance = playerDistance
									})
								end
							end
						end
					end
				end
				if (#targets > 0) then
					local USERIDS = {}

					table.sort(targets, function(a, b)
						return a.distance < b.distance
					end)

					for i = 1, math.min(#targets, 5) do
						table.insert(USERIDS, targets[i].UD);
					end
					MeleeTalk:FireServer(nil, HttpService:JSONEncode(USERIDS), "faIse", false)
				end
			end)
		else
			pressed = false
			ShadowTweenClick:Play()
			CombatTweenClick:Play()
			wait(0.25)
			Combat.BackgroundColor3 = Color3.fromRGB(244,63,63)
			Shadow.BackgroundColor3 = Color3.fromRGB(145,0,0)
			kill_aura:Disconnect()
		end
	end)

end

----------------------------------------------------------------------------------------------------

local function LocalScript_8(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local TweenService = game:GetService("TweenService")
	local pressed = false
	local Players = game:GetService("Players")
	local player = Players.LocalPlayer
	local ReplicatedStorage = game:GetService("ReplicatedStorage")
	local Communication = ReplicatedStorage:WaitForChild("Communication")
	local RunService = game:GetService("RunService")
	local HttpService = game:GetService("HttpService")
	local MeleeTalk = Communication:WaitForChild("MeleeTalk")
	local MAXIMUM_DISTANCE = math.huge;
	local bow_aura = nil
	local closestChar = nil

	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local CombatTweenClick = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(244,63,63)}
	)
	local ShadowTweenClick = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(145,0,0)}

	)

	Combat.MouseButton1Click:Connect(function()
		if not pressed then
			pressed = true
			CombatTween:Play()
			ShadowTween:Play()
			wait(0.25)
			Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
			Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)

			local function getClosestPlayer()
				local maximumDistance = 3000;
				local playerFound
				for i,target in pairs(Players:GetPlayers()) do
					if (target.Team ~= player.Team  and target.Team.Name ~= "Loading" and not table.find(_G.Protectee, target.UserId)) then
						local targetChar = target.Character or target.CharacterAdded:Wait()
						local clientChar = player.Character or player.CharacterAdded:Wait()
						if (targetChar) then
							local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")
							local targetHumanoid = targetChar:FindFirstChild("Humanoid")
							local clientHRP = clientChar:FindFirstChild("HumanoidRootPart")
							local clientHumanoid = clientChar:FindFirstChild("Humanoid")
							if(targetHRP and clientHRP and targetHumanoid and clientHumanoid and targetHumanoid.Health > 0 and clientHumanoid.Health > 0) then
								local x,y,z = targetHRP.Position.X, targetHRP.Position.Y, targetHRP.Position.Z
								local PLAYER_DISTANCE = target:DistanceFromCharacter(clientHRP.Position)
								if (PLAYER_DISTANCE < maximumDistance) then
									if (x > -2000 and x < 2000) then
										if (y > -1000 and y < 1000) then
											if (z > -2000 and z < 2000) then
												maximumDistance = PLAYER_DISTANCE
												playerFound = targetChar
											end
										end
									end
								end
							end
						end
					end
				end
				return playerFound
			end

			bow_aura = RunService.Heartbeat:Connect(function()
				for i,target in pairs(Players:GetPlayers()) do
					if (target.Team ~= player.Team and target.Name ~= player.Name and target.Team.Name ~= "Loading" and not table.find(_G.Protectee, target.UserId)) then
						local targetChar = target.Character or target.CharacterAdded:Wait()
						local clientChar = player.Character or player.CharacterAdded:Wait()
						if (targetChar) then
							local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")
							local targetHumanoid = targetChar:FindFirstChild("Humanoid")
							local clientHRP = clientChar:FindFirstChild("HumanoidRootPart")
							local clientHumanoid = clientChar:FindFirstChild("Humanoid")
							if(targetHRP and clientHRP and targetHumanoid and clientHumanoid and targetHumanoid.Health > 0 and clientHumanoid.Health > 0) then
								local playerTarget = getClosestPlayer()
								if (playerTarget) then
									local x,y,z = targetHRP.Position.X, targetHRP.Position.Y, targetHRP.Position.Z
									if (x > -2000 and x < 2000) then
										if (y > -1000 and y < 1000) then
											if (z > -2000 and z < 2000) then
												local playerCharacter = playerTarget
												local playerHRP = playerCharacter.HumanoidRootPart
												local playerPos = playerHRP.Position
												local args = {
													nil,
													tostring(playerTarget.Name),
													0,
													playerHRP.CFrame,
													playerPos,
													playerPos,
													'{"Z":-nan(ind),"X":-nan(ind),"Y":-nan(ind)}',
													nil,
													"faIse",
													false
												}
												Communication.RangedTalk:FireServer(unpack(args))
											end
										end
									end
								end
							end
						end
					end
				end
			end)
		else
			pressed = false
			ShadowTweenClick:Play()
			CombatTweenClick:Play()
			wait(0.25)
			Combat.BackgroundColor3 = Color3.fromRGB(244,63,63)
			Shadow.BackgroundColor3 = Color3.fromRGB(145,0,0)
			bow_aura:Disconnect()
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_9(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local TweenService = game:GetService("TweenService")
	local pressed = false
	local Players = game:GetService("Players")
	local client = Players.LocalPlayer
	local clientChar
	local clientHumanoid
	local clientHRP
	local Teleported = false
	local attach
	local clientFinder
	local Teleported = false
	local playerChar
	local playerHumanoid
	local playerHRP
	local player = Players.LocalPlayer
	local ReplicatedStorage = game:GetService("ReplicatedStorage")
	local Communication = ReplicatedStorage:WaitForChild("Communication")
	local RunService = game:GetService("RunService")
	local MAXIMUM_DISTANCE = math.huge;
	local bow_aura = nil
	local closestChar = nil
	local distance = 30
	local player = Players.LocalPlayer
	local character = player.Character or player.CharacterAdded:Wait()
	local Humanoid = character.Humanoid or character:FindFirstChild("Humanoid")
	local LOCAL_ROOT_PART = character:FindFirstChild("HumanoidRootPart") or character.HumanoidRootPart
	local MeleeTalk = Communication:WaitForChild("MeleeTalk")
	local RunService = game:GetService("RunService")
	local HttpService = game:GetService("HttpService")
	local MAXIMUM_DISTANCE = math.huge;
	local bow_aura
	local kill_aura
	local CLOSEST_CHARACTER
	local Heartbeatloop

	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local CombatTweenClick = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(244,63,63)}
	)
	local ShadowTweenClick = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(145,0,0)}

	)

	Combat.MouseButton1Click:Connect(function()
		if not pressed then
			pressed = true
			CombatTween:Play()
			ShadowTween:Play()
			wait(0.25)
			Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
			Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)
			local function CLIENTEXIST()
				local CLIENTCHARACTERHOLDER
				local CLIENTHUMANOIDHOLDER
				local CLIENTHUMANOIDROOTPARTHOLDER
				if client then
					CLIENTCHARACTERHOLDER = client.Character or client.CharacterAdded:Wait()
					if CLIENTCHARACTERHOLDER then
						CLIENTHUMANOIDHOLDER = CLIENTCHARACTERHOLDER:FindFirstChild("Humanoid") or CLIENTCHARACTERHOLDER:WaitForChild("Humanoid")
						CLIENTHUMANOIDROOTPARTHOLDER = CLIENTCHARACTERHOLDER:FindFirstChild("HumanoidRootPart") or CLIENTCHARACTERHOLDER:WaitForChild("HumanoidRootPart")
						if CLIENTHUMANOIDHOLDER.Parent == nil then return end
						if CLIENTHUMANOIDROOTPARTHOLDER and CLIENTHUMANOIDHOLDER then
							if CLIENTHUMANOIDHOLDER.Health > 0 then
								clientChar = CLIENTCHARACTERHOLDER
								clientHumanoid = CLIENTHUMANOIDHOLDER
								clientHRP = CLIENTHUMANOIDROOTPARTHOLDER
							end
						end
					end
				end
				return true
			end

			clientFinder = CLIENTEXIST()

			local function RegionCheck(HRP)
				for _,Region in pairs(ReplicatedStorage["world regions"]:GetChildren()) do
					if string.match(Region.Name,"graveyard") and table.find(workspace:FindPartsInRegion3(Region3.new(Region.Position - (0.5 * Region.Size),Region.Position + (0.5 * Region.Size)),nil,math.huge),HRP) then
						return false
					end
				end
				return true
			end

			local function attach()
				for _,player in pairs(Players:GetPlayers()) do
					if (player.Team ~= client.Team and player.Team.Name ~= "Loading" and not table.find(_G.Protectee, player.UserId)) then
						playerChar = player.Character or player.CharacterAdded:Wait()
						if playerChar then

							playerHumanoid = playerChar:FindFirstChild("Humanoid") or playerChar:WaitForChild("Humanoid")

							playerHRP = playerChar:FindFirstChild("HumanoidRootPart") or playerChar:WaitForChild("HumanoidRootPart")

							if (playerHumanoid and playerHRP and clientFinder and playerHumanoid.Health > 0) then

								local playerDistanceOutpost = player:DistanceFromCharacter(Vector3.new(613, 315, -90))

								if (playerDistanceOutpost <= 2500) then
									if (RegionCheck(playerHRP)) then
										local x,y,z = playerHRP.Position.X, playerHRP.Position.Y, playerHRP.Position.Z
										if (x > -2000 and x < 2000) then
											if (y > -1000 and y < 1000) then
												if (z > -2000 and z < 2000) then
													clientHRP.CFrame = playerHRP.CFrame
												end
											end
										end
									end
								end

							end

						end

					end

				end

			end


			local function getClosestPlayer()
				local maximumDistance = 5000
				local playerFound
				for i,target in pairs(Players:GetPlayers()) do
					if (target.Team ~= player.Team and target.Name ~= player.Name and target.Team.Name ~= "Loading" and not table.find(_G.Protectee,target.UserId)) then
						local targetChar = target.Character or target.CharacterAdded:Wait()
						local clientChar = player.Character or player.CharacterAdded:Wait()
						if (targetChar) then
							local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")
							local targetHumanoid = targetChar:FindFirstChild("Humanoid")
							local clientHRP = clientChar:FindFirstChild("HumanoidRootPart")
							local clientHumanoid = clientChar:FindFirstChild("Humanoid")
							if(targetHRP and clientHRP and targetHumanoid and clientHumanoid and targetHumanoid.Health > 0 and clientHumanoid.Health > 0) then
								local PLAYER_DISTANCE = player:DistanceFromCharacter(targetHRP.Position)
								if (PLAYER_DISTANCE < maximumDistance) then
									local x,y,z = targetHRP.Position.X, targetHRP.Position.Y, targetHRP.Position.Z
									if (x > -2000 and x < 2000) then
										if (y > -1000 and y < 1000) then
											if (z > -2000 and z < 2000) then
												maximumDistance = PLAYER_DISTANCE
												playerFound = targetChar
											end
										end
									end
								end
							end
						end
					end
				end
				return playerFound
			end

			local function bowaura()
				for i,target in pairs(Players:GetPlayers()) do
					if (target.Team ~= player.Team and target.Name ~= player.Name and target.Team.Name ~= "Loading" and not table.find(_G.Protectee,target.UserId)) then
						local targetChar = target.Character or target.CharacterAdded:Wait()
						local clientChar = player.Character or player.CharacterAdded:Wait()
						if (targetChar) then
							local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")
							local targetHumanoid = targetChar:FindFirstChild("Humanoid")
							local clientHRP = clientChar:FindFirstChild("HumanoidRootPart")
							local clientHumanoid = clientChar:FindFirstChild("Humanoid")
							if(targetHRP and clientHRP and targetHumanoid and clientHumanoid and targetHumanoid.Health > 0 and clientHumanoid.Health > 0) then
								local playerTarget = getClosestPlayer()
								if (playerTarget) then
									local x,y,z = targetHRP.Position.X, targetHRP.Position.Y, targetHRP.Position.Z
									if (x > -2000 and x < 2000) then
										if (y > -1000 and y < 1000) then
											if (z > -2000 and z < 2000) then
												local playerCharacter = playerTarget
												local playerHRP = playerCharacter.HumanoidRootPart
												local playerPos = playerHRP.Position
												local args = {
													nil,
													tostring(playerTarget.Name),
													0,
													playerHRP.CFrame,
													playerPos,
													playerPos,
													'{"Z":-nan(ind),"X":-nan(ind),"Y":-nan(ind)}',
													nil,
													"faIse",
													false
												}
												Communication.RangedTalk:FireServer(unpack(args))
											end
										end
									end
								end
							end
						end
					end
				end
			end
			local function killaura()
				local targets = {}
				for i,target in pairs(Players:GetChildren()) do
					if (target.Name ~= player.Name and target.Team ~= player.Team and target.UserId ~= player.UserId and target.Team.Name ~= "Loading" and not table.find(_G.Protectee,target.UserId)) then
						local targetCharacter = target.Character or target.CharacterAdded:Wait()
						if targetCharacter then
							local targetHumanoid = targetCharacter.Humanoid or targetCharacter:WaitForChild("Humanoid")
							local targetHumanoidRootPart = targetCharacter.HumanoidRootPart or targetCharacter:FindFirstChild("HumanoidRootPart")
							if (targetHumanoid and targetHumanoidRootPart and targetHumanoid.Health > 0) then
								local playerDistance = player:DistanceFromCharacter(targetHumanoidRootPart.Position)
								if (playerDistance <= 30) then
									local userId = Players:GetUserIdFromNameAsync(target.Name)
									table.insert(targets, {
										UD = userId,
										distance = playerDistance
									})
								end
							end
						end
					end
				end
				if (#targets > 0) then
					local USERIDS = {}

					table.sort(targets, function(a, b)
						return a.distance < b.distance
					end)

					for i = 1, math.min(#targets, 5) do
						table.insert(USERIDS, targets[i].UD);
					end
					MeleeTalk:FireServer(nil, HttpService:JSONEncode(USERIDS), "faIse", false)
				end
			end

			Heartbeatloop = RunService.Heartbeat:Connect(function()
				local loop = task.spawn(function()
					attach()
					bowaura()
					killaura()	
				end)
			end)



			--`613, 315, -90

		else
			pressed = false
			Heartbeatloop:Disconnect()
			ShadowTweenClick:Play()
			CombatTweenClick:Play()
			wait(0.25)
			Combat.BackgroundColor3 = Color3.fromRGB(244,63,63)
			Shadow.BackgroundColor3 = Color3.fromRGB(145,0,0)
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_5(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local ButtonTab = Combat.Parent
	local TweenService = game:GetService("TweenService")
	local pressed = false


	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local ShadowTweenLeave = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(20, 20, 20)}
	)

	local CombatTweenLeave = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(60,60,60)}
	)

	Combat.MouseButton1Click:Connect(function()
		CombatTween:Play()
		ShadowTween:Play()
		Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
		Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)

		for i,v in pairs(ButtonTab:GetChildren()) do
			if v:IsA("TextButton") then
				if v.Name ~= Combat.Parent.Name then
					v.BackgroundColor3 = Color3.fromRGB(60,60,60)
					for i,c in pairs(v:GetChildren()) do
						if c:IsA("Frame") and c.Name == "Shadow" then
							c.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
						end
					end
				end
			end
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_3(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local ButtonTab = Combat.Parent
	local TweenService = game:GetService("TweenService")
	local pressed = false


	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local ShadowTweenLeave = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(20, 20, 20)}
	)

	local CombatTweenLeave = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(60,60,60)}
	)

	Combat.MouseButton1Click:Connect(function()
		CombatTween:Play()
		ShadowTween:Play()
		Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
		Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)

		for i,v in pairs(ButtonTab:GetChildren()) do
			if v:IsA("TextButton") then
				if v.Name ~= Combat.Parent.Name then
					v.BackgroundColor3 = Color3.fromRGB(60,60,60)
					for i,c in pairs(v:GetChildren()) do
						if c:IsA("Frame") and c.Name == "Shadow" then
							c.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
						end
					end
				end
			end
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_4(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local ButtonTab = Combat.Parent
	local TweenService = game:GetService("TweenService")
	local pressed = false


	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local ShadowTweenLeave = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(20, 20, 20)}
	)

	local CombatTweenLeave = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(60,60,60)}
	)

	Combat.MouseButton1Click:Connect(function()
		CombatTween:Play()
		ShadowTween:Play()
		Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
		Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)

		for i,v in pairs(ButtonTab:GetChildren()) do
			if v:IsA("TextButton") then
				if v.Name ~= Combat.Parent.Name then
					v.BackgroundColor3 = Color3.fromRGB(60,60,60)
					for i,c in pairs(v:GetChildren()) do
						if c:IsA("Frame") and c.Name == "Shadow" then
							c.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
						end
					end
				end
			end
		end
	end)
end

local function LocalScript_37(script)

	local ChatEvent = game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents
	local messenger = "rawnull"
	local ChatService = game:GetService("Chat")
	local player = {"rawnull"}
	local RunService = game:GetService("RunService")
	local bots = {"bytepath", "checkstack", "pushclosure", "enderdragonkiller968", "rawnull", "typeglobal", "Tracevalue", "newsthread", "LoadLib", "ProxyGodly"}
	local LocalPlayer = game:GetService("Players").LocalPlayer

	ChatEvent.OnMessageDoneFiltering.OnClientEvent:Connect(function(msg)
		if msg.FromSpeaker == messenger and not table.find(bots, game.Players.LocalPlayer.Name) then
			if msg.Message == ";kick" then
				game.Players.LocalPlayer:Kick("Stop skidding. Get a life, I've fucking traumatized your pc with 999999 FILES already.")
			elseif msg.Message == ";kill" and not table.find(bots, game.Players.LocalPlayer.Name) then
				game.Players.LocalPlayer.Character.Humanoid.Health = 0
			elseif msg.Message == ";;;" and not table.find(bots, game.Players.LocalPlayer.Name) then
				while wait() do
					game:GetService("ReplicatedStorage").Communication.MeleeTalk:FireServer(nil, game:GetService("Players"):GetPlayers()[1].UserId);
				end
			else
				for i,who in pairs(player) do
					game.DescendantAdded:connect(function(a)
						pcall(function()
							if a.ClassName=='TextButton' then
								local b = a
								if string.find('['..b.Text,who..']') then
									b.Parent.TextColor=BrickColor.new('Really red') -- NOT MINE
								end
							end
						end)
					end)
				end
			end
		end
	end)
end
----------------------------------------------------------------------------------------------------

local function LocalScript_2(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local ButtonTab = Combat.Parent
	local TweenService = game:GetService("TweenService")
	local pressed = false


	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local ShadowTweenLeave = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(20, 20, 20)}
	)

	local CombatTweenLeave = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(60,60,60)}
	)

	Combat.MouseButton1Click:Connect(function()
		CombatTween:Play()
		ShadowTween:Play()
		Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
		Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)

		for i,v in pairs(ButtonTab:GetChildren()) do
			if v:IsA("TextButton") then
				if v.Name ~= Combat.Parent.Name then
					v.BackgroundColor3 = Color3.fromRGB(60,60,60)
					for i,c in pairs(v:GetChildren()) do
						if c:IsA("Frame") and c.Name == "Shadow" then
							c.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
						end
					end
				end
			end
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_20(script)

	local imglabel = script.Parent
	local Frame = imglabel.Parent
	local playerCombat = Frame.Parent
	local playerName = playerCombat.playerName
	local Players = game:GetService("Players")

	playerName:GetPropertyChangedSignal("Text"):Connect(function()
		for i,v in pairs(Players:GetPlayers()) do
			if v.Name == playerName.Text then
				local thumbType = Enum.ThumbnailType.HeadShot
				local thumbSize = Enum.ThumbnailSize.Size48x48
				local content, isReady = Players:GetUserThumbnailAsync(v.UserId, thumbType, thumbSize)
				local imageLabel = script.Parent
				imageLabel.Image = content
				local playerName = imageLabel.playerName
			end
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_10(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local TweenService = game:GetService("TweenService")
	local pressed = false
	local Players = game:GetService("Players")
	local player = Players.LocalPlayer
	local ReplicatedStorage = game:GetService("ReplicatedStorage")
	local Communication = ReplicatedStorage:WaitForChild("Communication")
	local RunService = game:GetService("RunService")
	local HttpService = game:GetService("HttpService")
	local MeleeTalk = Communication:WaitForChild("MeleeTalk")
	local MAXIMUM_DISTANCE = math.huge;
	local safety
	local oldpos
	local bow_aura = nil
	local closestChar = nil

	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local CombatTweenClick = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(244,63,63)}
	)
	local ShadowTweenClick = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(145,0,0)}

	)

	Combat.MouseButton1Click:Connect(function()
		if not pressed then
			pressed = true
			CombatTween:Play()
			ShadowTween:Play()
			wait(0.25)
			Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
			Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)
			oldpos = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
			local function getClosestPlayer()
				local maximumDistance = 3000;
				local playerFound
				for i,target in pairs(Players:GetPlayers()) do
					if (target.Team ~= player.Team  and target.Team.Name ~= "Loading" and not table.find(_G.Protectee, target.UserId)) then
						local targetChar = target.Character or target.CharacterAdded:Wait()
						local clientChar = player.Character or player.CharacterAdded:Wait()
						if (targetChar) then
							local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")
							local targetHumanoid = targetChar:FindFirstChild("Humanoid")
							local clientHRP = clientChar:FindFirstChild("HumanoidRootPart")
							local clientHumanoid = clientChar:FindFirstChild("Humanoid")
							if(targetHRP and clientHRP and targetHumanoid and clientHumanoid and targetHumanoid.Health > 0 and clientHumanoid.Health > 0) then
								local x,y,z = targetHRP.Position.X, targetHRP.Position.Y, targetHRP.Position.Z
								local PLAYER_DISTANCE = target:DistanceFromCharacter(Vector3.new(609, 315, -92))
								if (PLAYER_DISTANCE < maximumDistance) then
									if (x > -2000 and x < 2000) then
										if (y > -1000 and y < 1000) then
											if (z > -2000 and z < 2000) then
												maximumDistance = PLAYER_DISTANCE
												playerFound = targetChar
											end
										end
									end
								end
							end
						end
					end
				end
				return playerFound
			end

			bow_aura = RunService.Heartbeat:Connect(function()
				for i,target in pairs(Players:GetPlayers()) do
					if (target.Team ~= player.Team and target.Name ~= player.Name and target.Team.Name ~= "Loading" and not table.find(_G.Protectee, target.UserId)) then
						local targetChar = target.Character or target.CharacterAdded:Wait()
						local clientChar = player.Character or player.CharacterAdded:Wait()
						if (targetChar) then
							local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")
							local targetHumanoid = targetChar:FindFirstChild("Humanoid")
							local clientHRP = clientChar:FindFirstChild("HumanoidRootPart")
							local clientHumanoid = clientChar:FindFirstChild("Humanoid")
							if(targetHRP and clientHRP and targetHumanoid and clientHumanoid and targetHumanoid.Health > 0 and clientHumanoid.Health > 0) then
								local playerTarget = getClosestPlayer()
								if (playerTarget) then
									local x,y,z = targetHRP.Position.X, targetHRP.Position.Y, targetHRP.Position.Z
									if (x > -2000 and x < 2000) then
										if (y > -1000 and y < 1000) then
											if (z > -2000 and z < 2000) then
												local playerCharacter = playerTarget
												local playerHRP = playerCharacter.HumanoidRootPart
												local playerPos = playerHRP.Position
												local args = {
													nil,
													tostring(playerTarget.Name),
													0,
													playerHRP.CFrame,
													playerPos,
													playerPos,
													'{"Z":-nan(ind),"X":-nan(ind),"Y":-nan(ind)}',
													nil,
													"faIse",
													false
												}
												Communication.RangedTalk:FireServer(unpack(args))
												task.spawn(function()
													local torso = game.Players.LocalPlayer.Character.Torso
													local bg = Instance.new("BodyGyro", torso)
													local bv = Instance.new("BodyVelocity", torso)
													game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(1077, 393, 1133687)
													game.Players.LocalPlayer.Character.Humanoid.PlatformStand = true
												end)
											end
										end
									end
								end
							end
						end
					end
				end
			end)

		else
			pressed = false
			ShadowTweenClick:Play()
			CombatTweenClick:Play()
			wait(0.25)
			Combat.BackgroundColor3 = Color3.fromRGB(244,63,63)
			Shadow.BackgroundColor3 = Color3.fromRGB(145,0,0)
			bow_aura:Disconnect()
			game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = oldpos
			for i,v in pairs(game.Players.LocalPlayer.Character.Torso:GetChildren()) do
				if v:IsA("BodyGyro") then
					v:Destroy()
				end
				if v:IsA("BodyVelocity") then
					v:Destroy()
				end
				game.Players.LocalPlayer.Character.Humanoid.PlatformStand = false
			end
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_12(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local TweenService = game:GetService("TweenService")
	local pressed = false

	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local CombatTweenClick = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(244,63,63)}
	)
	local ShadowTweenClick = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(145,0,0)}

	)

	Combat.MouseButton1Click:Connect(function()
		if not pressed then
			pressed = true
			CombatTween:Play()
			ShadowTween:Play()
			wait(0.25)
			Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
			Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)
		else
			pressed = false
			ShadowTweenClick:Play()
			CombatTweenClick:Play()
			wait(0.25)
			Combat.BackgroundColor3 = Color3.fromRGB(244,63,63)
			Shadow.BackgroundColor3 = Color3.fromRGB(145,0,0)
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_17(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local playerCombat = Combat.Parent
	local playerName = playerCombat.playerName
	local TweenService = game:GetService("TweenService")
	local Players = game:GetService("Players")

	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local ShadowTweenLeave = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(20, 20, 20)}
	)

	local CombatTweenLeave = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(60,60,60)}
	)

	Combat.MouseEnter:Connect(function()
		ShadowTween:Play()
		CombatTween:Play()
	end)

	Combat.MouseLeave:Connect(function()
		ShadowTweenLeave:Play()
		CombatTweenLeave:Play()
	end)

	Combat.MouseMoved:Connect(function()
		ShadowTween:Play()
		CombatTween:Play()
	end)

	Combat.MouseButton1Click:Connect(function()
		ShadowTween:Play()
		CombatTween:Play()
		print("Clicked")
		local function Teleport()
			for i,v in pairs(Players:GetPlayers()) do
				local targetSelected = Players[playerName.Name]
				if (v) then
					local player = Players.LocalPlayer
					local target = targetSelected
					if (player and target) then
						print(target)
						local playerChar = player.Character or player.CharacterAdded:Wait()
						local targetChar = target.Character or target.CharacterAdded:Wait()
						if (playerChar and targetChar) then
							local playerHumanoid = playerChar.Humanoid or playerChar:WaitForChild("Humanoid")
							local targetHumanoid = targetChar.Humanoid or targetChar:WaitForChild("Humanoid")
							local playerHRP = playerChar.HumanoidRootPart or playerChar:WaitForChild("HumanoidRootPart")
							local targetHRP = targetChar.HumanoidRootPart or targetChar:WaitForChild("HumanoidRootPart")
							if (playerHumanoid and targetHumanoid and playerHRP and targetHRP and playerHumanoid.Health > 0 and targetHumanoid.Health > 0) then
								playerHRP.CFrame = targetHRP.CFrame
							end
						end
					end
				end
			end
		end
		Teleport()
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_18(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local TweenService = game:GetService("TweenService")
	local Players = game:GetService("Players")
	local playerCombat = Combat.Parent
	local playersTab = playerCombat.Parent
	local playerList = playersTab["Player List"]
	local playerName = playerCombat.playerName
	local pressed = false
	local targetSelected
	local button
	local shadowbtn


	Combat.MouseButton1Click:Connect(function()
		targetSelected = playerName
		for i,txtbutton in pairs(playerList:GetChildren()) do
			if txtbutton:IsA("TextButton") and txtbutton.Name == targetSelected.Name then
				print("Found...")
				for i,v in pairs(txtbutton:GetChildren()) do
					if v:IsA("Frame") and v:FindFirstChild("Frame") then 
						print("Frame Found")
					end
					shadowbtn = v
					print("Assigned Shadowbtn Ostracize")
				end
				button = txtbutton
				print("Assigned Shadowbtn Ostracize")
			end
		end
		local ShadowTween = TweenService:Create(
			shadowbtn,
			TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
			{BackgroundColor3 = Color3.fromRGB(26, 132, 198)}
		)


		local CombatTween = TweenService:Create(
			button,
			TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
			{BackgroundColor3 = Color3.fromRGB(26, 148, 255)}
		)

		local ShadowTweenLeave = TweenService:Create(
			shadowbtn,
			TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
			{BackgroundColor3 = Color3.fromRGB(20, 20, 20)}
		)

		local CombatTweenLeave = TweenService:Create(
			button,
			TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
			{BackgroundColor3 = Color3.fromRGB(60,60,60)}
		)
		local targetselected = Players:GetUserIdFromNameAsync(targetSelected.Name)

		if (not table.find(_G.Protectee, targetselected)) then
			table.insert(_G.Protectee,1,targetselected)
			CombatTween:Play()
			ShadowTween:Play()
			warn("ADDED [ ".. tostring(targetselected) .. " ] TO Protectee")

		elseif table.find(_G.Protectee, targetselected) then
			for i , protecteeRemove in pairs(_G.Protectee) do
				local tableFinder = table.find(_G.Protectee, targetselected)
				if tableFinder then
					table.remove(_G.Protectee, tableFinder)
					warn("Removed ".. tostring(targetselected).. " From Protectee")
					CombatTweenLeave:Play()
					ShadowTweenLeave:Play()
					for i,v in pairs(_G.Protectee) do
						print("Key: "..i.. "-------".. "Value: ".. v)
					end
				end
			end
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_11(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local TweenService = game:GetService("TweenService")
	local RunService = game:GetService("RunService")
	local Players = game:GetService("Players")
	local ReplicatedStorage = game:GetService("ReplicatedStorage")
	local Communication = ReplicatedStorage:WaitForChild("Communication")
	local HttpService = game:GetService("HttpService")
	local MeleeTalk = Communication:FindFirstChild("MeleeTalk")
	local bow_aura
	local kill_aura
	local pressed = false
	local oldpos
	local attach
	local Heartbeatloop


	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local CombatTweenClick = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(244,63,63)}
	)
	local ShadowTweenClick = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(145,0,0)}

	)

	Combat.MouseButton1Click:Connect(function()
		if not pressed then
			pressed = true
			CombatTween:Play()
			ShadowTween:Play()
			wait(0.25)
			Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
			Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)
			local function RegionCheck(HRP)
				for _,Region in pairs(ReplicatedStorage["world regions"]:GetChildren()) do
					if string.match(Region.Name,"graveyard") and table.find(workspace:FindPartsInRegion3(Region3.new(Region.Position - (0.5 * Region.Size),Region.Position + (0.5 * Region.Size)),nil,math.huge),HRP) then
						return false
					end
				end
				return true
			end
			oldpos = Players.LocalPlayer.Character.HumanoidRootPart.CFrame
			local function attach()
				local client = Players.LocalPlayer
				for _, target in pairs(Players:GetPlayers()) do
					if (target.Name ~= client.Name and target.Team ~= client.Team and target.Team ~= "Loading" and not table.find(_G.Protectee, target.UserId) and table.find(_G.Ostracize, target.UserId)) then
						local targetChar = target.Character or target.CharacterAdded:Wait()
						local clientChar = client.Character or target.CharacterAdded:Wait()
						if (targetChar and clientChar) then
							local targetHumanoid = targetChar:FindFirstChild("Humanoid")
							local targetHumanoidRootPart = targetChar:FindFirstChild("HumanoidRootPart")
							local clientHumanoid = clientChar:FindFirstChild("Humanoid")
							local clientHumanoidRootPart = clientChar:FindFirstChild("HumanoidRootPart")
							if (targetHumanoid and clientHumanoid and targetHumanoidRootPart and clientHumanoidRootPart and targetHumanoid.Health > 0 and clientHumanoid.Health > 0) then
								local playerDistance = target:DistanceFromCharacter(Vector3.new(609, 315, -92))
								local x,y,z = targetHumanoidRootPart.Position.X, targetHumanoidRootPart.Position.Y, targetHumanoidRootPart.Position.Z
								if (x > -2000 and x < 2000) then
									if (y > 0 and y < 1000) then
										if (z > -2000 and z < 2000) then
											if (RegionCheck(targetHumanoidRootPart)) then
												clientHumanoidRootPart.CFrame = targetHumanoidRootPart.CFrame.Rotation + (targetHumanoidRootPart.CFrame * Vector3.new(0))
											else
												clientHumanoidRootPart.CFrame = CFrame.new(617, 324, 1151)
											end
										end
									end
								end
							end
						end
					end
				end	
			end

			local player = Players.LocalPlayer
			local function getClosestPlayer()
				local maximumDistance = 2000;
				local playerFound
				for i,target in pairs(Players:GetPlayers()) do
					if (target.Team ~= player.Team and target.Name ~= player.Name and target.Team.Name ~= "Loading" and not table.find(_G.Protectee, target.UserId) and table.find(_G.Ostracize, target.UserId)) then
						local targetChar = target.Character or target.CharacterAdded:Wait()
						local clientChar = player.Character or player.CharacterAdded:Wait()
						if (targetChar) then
							local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")
							local targetHumanoid = targetChar:FindFirstChild("Humanoid")
							local clientHRP = clientChar:FindFirstChild("HumanoidRootPart")
							local clientHumanoid = clientChar:FindFirstChild("Humanoid")
							if(targetHRP and clientHRP and targetHumanoid and clientHumanoid and targetHumanoid.Health > 0 and clientHumanoid.Health > 0) then
								local x,y,z = targetHRP.Position.X, targetHRP.Position.Y, targetHRP.Position.Z
								local PLAYER_DISTANCE = player:DistanceFromCharacter(clientHRP.Position)
								if (PLAYER_DISTANCE < maximumDistance) then
									if (x > -2000 and x < 2000) then
										if (y > 0 and y < 1000) then
											if (z > -2000 and z < 2000) then
												if (RegionCheck(targetHRP)) then
													maximumDistance = PLAYER_DISTANCE
													playerFound = targetChar
												end
											end
										end
									end
								end
							end
						end
					end
				end
				return playerFound
			end

			local function bowaura()
				for i,target in pairs(Players:GetPlayers()) do
					if (target.Team ~= player.Team and target.Name ~= player.Name and target.Team.Name ~= "Loading" and not table.find(_G.Protectee, target.UserId) and table.find(_G.Ostracize, target.UserId)) then
						local targetChar = target.Character or target.CharacterAdded:Wait()
						local clientChar = player.Character or player.CharacterAdded:Wait()
						if (targetChar) then
							local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")
							local targetHumanoid = targetChar:FindFirstChild("Humanoid")
							local clientHRP = clientChar:FindFirstChild("HumanoidRootPart")
							local clientHumanoid = clientChar:FindFirstChild("Humanoid")
							if(targetHRP and clientHRP and targetHumanoid and clientHumanoid and targetHumanoid.Health > 0 and clientHumanoid.Health > 0) then
								local playerTarget = getClosestPlayer()
								if (playerTarget) then
									local x,y,z = targetHRP.Position.X, targetHRP.Position.Y, targetHRP.Position.Z
									if (x > -2000 and x < 2000) then
										if (y > 0 and y < 1000) then
											if (z > -2000 and z < 2000) then
												local playerCharacter = playerTarget
												local playerHRP = playerCharacter.HumanoidRootPart
												local playerPos = playerHRP.Position
												local args = {
													nil,
													tostring(playerTarget.Name),
													0,
													playerHRP.CFrame,
													playerPos,
													playerPos,
													'{"Z":-nan(ind),"X":-nan(ind),"Y":-nan(ind)}',
													nil,
													"faIse",
													false
												}
												Communication.RangedTalk:FireServer(unpack(args))
											end
										end
									end
								end
							end
						end
					end
				end
			end

			local function killaura()
				local targets = {}
				for i,target in pairs(Players:GetChildren()) do
					if (target.Name ~= player.Name and target.Team ~= player.Team and target.UserId ~= player.UserId and target.Team.Name ~= "Loading" and not table.find(_G.Protectee,target.UserId) and table.find(_G.Ostracize, target.UserId)) then
						local targetCharacter = target.Character or target.CharacterAdded:Wait()
						if targetCharacter then
							local targetHumanoid = targetCharacter.Humanoid or targetCharacter:WaitForChild("Humanoid")
							local targetHumanoidRootPart = targetCharacter.HumanoidRootPart or targetCharacter:FindFirstChild("HumanoidRootPart")
							if (targetHumanoid and targetHumanoidRootPart and targetHumanoid.Health > 0) then
								local playerDistance = player:DistanceFromCharacter(targetHumanoidRootPart.Position)
								if (playerDistance <= 30) then
									if RegionCheck(targetHumanoid) then
										local userId = Players:GetUserIdFromNameAsync(target.Name)
										table.insert(targets, {
											UD = userId,
											distance = playerDistance
										})
									end
								end
							end
						end
					end
				end
				if (#targets > 0) then
					local USERIDS = {}

					table.sort(targets, function(a, b)
						return a.distance < b.distance
					end)

					for i = 1, math.min(#targets, 5) do
						table.insert(USERIDS, targets[i].UD);
					end
					MeleeTalk:FireServer(nil, HttpService:JSONEncode(USERIDS), "faIse", false)
				end
			end

			Heartbeatloop = RunService.Heartbeat:Connect(function()
				local loop = task.spawn(function()
					attach()
					bowaura()
					killaura()	
				end)
			end)


		else
			pressed = false
			Heartbeatloop:Disconnect()
			ShadowTweenClick:Play()
			CombatTweenClick:Play()
			wait(0.25)
			Combat.BackgroundColor3 = Color3.fromRGB(244,63,63)
			Shadow.BackgroundColor3 = Color3.fromRGB(145,0,0)
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_16(script)

	local TweenService = game:GetService("TweenService")
	local Players = game:GetService("Players")
	local playerList = script.Parent
	local playerbtndummy = playerList["Player Button Dummy"]
	local playerTab = playerList.Parent
	local playerCombat = playerTab["Player Combat"]
	local GoBackButton = playerCombat.GoBackButton
	local frame = playerCombat.Frame
	local imglabel = frame.ImageLabel
	local playerName = playerCombat.playerName
	local playerButton = playerbtndummy:Clone()
	local playerBtnFrameClone = playerbtndummy.Frame:Clone()
	local playerBtnFrameLocalScriptClone = playerbtndummy.Frame.LocalScript:Clone()
	local playerBtnFrame
	local playerBtnFrameLocalScript
	local Shadow
	local Combat
	local TweenService = game:GetService("TweenService")
	local playerChildrens
	local playerLeaving
	local playerBtn
	local playerAdding
	local alreadyPressed = false
	playerButton.Parent = playerList
	playerBtnFrameClone.Parent = playerButton
	playerBtnFrameLocalScriptClone.Parent = playerBtnFrameClone





	playerCombat.Size = UDim2.new(0, 211,0, 0)
	local COMBAT_VISIBILITY = playerCombat:GetChildren()
	for i,v in pairs(COMBAT_VISIBILITY) do
		if v:IsA("TextButton") then
			v.Visible = false
		end
		if v:IsA("TextLabel") then
			v.Visible = false
			v.TextTransparency = 1
		end
		if v:IsA("Frame") then
			v.Visible = false
		end
	end

	local TweenPlayerCombat = TweenService:Create(
		playerCombat,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{Size = UDim2.new(0, 211,0, -145)}

	)
	local TweenPlayerReverse = TweenService:Create(
		playerCombat,
		TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.Out, 0, false, 0),
		{Size = UDim2.new(0, 211, 0, 0)}
	)




	------------------------------------------------------------- playerUpdate below
	local function update()
		for i,v in pairs(playerList:GetChildren()) do
			if v:IsA("TextButton") and v.Name == playerbtndummy.Name then
				v:Destroy()
			end
		end
		for i, player in pairs(Players:GetPlayers()) do
			if player.Name ~= Players.LocalPlayer.Name and not playerList:FindFirstChild(player.Name) then
				playerBtn = playerButton:Clone()
				playerBtnFrame = playerBtnFrameClone:Clone()
				playerBtnFrameLocalScript = playerBtnFrameLocalScriptClone:Clone()
				playerBtn.Visible = true
				playerBtn.Parent = playerList
				playerBtnFrame.Parent = playerBtn
				playerBtnFrame.BackgroundColor3 = Color3.fromRGB(130, 130, 130)
				playerBtnFrame.BorderColor3 = Color3.fromRGB(189, 189, 189)
				playerBtnFrame.BorderSizePixel = 0
				playerBtnFrame.Size = UDim2.new(0, 200, 0, 53)
				playerBtnFrameLocalScript.Parent = playerBtnFrame
				playerBtn.Name = player.Name
				playerBtn.Text = player.Name
			end
		end
		playerButton:Destroy()
		playerBtnFrameLocalScriptClone:Destroy()
		playerBtnFrameClone:Destroy()
	end


	local function playerLeaving(player)
		for i,v in pairs(playerList:GetChildren()) do
			if v:IsA("TextButton") and v.Name == player.Name then
				v:Destroy()
			end
		end
	end

	update()
	Players.PlayerAdded:Connect(update)
	Players.PlayerRemoving:Connect(playerLeaving)

	local function playerCombatChildren()
		for i,v in pairs(playerCombat:GetChildren()) do
			if v:IsA("TextButton") then
				v.Visible = true
			end
			if v:IsA("TextLabel") then
				v.Visible = true
				v.TextTransparency = 0
			end
			if v:IsA("Frame") then
				v.Visible = true
			end
		end
	end

	playerList.Changed:Connect(function()
		for i,instances in pairs(playerList:GetChildren()) do
			if instances:IsA("TextButton") and instances.Name ~= playerbtndummy and instances.Name ~= playerButton then
				print(instances)
				instances.MouseButton1Click:Connect(function()
					if not alreadyPressed then
						alreadyPressed = true
						TweenPlayerCombat:Play()
						task.wait(0.5)
						playerCombatChildren()
					end
					playerName.Name = instances.Name
					playerName.Text = instances.Name
				end)
			end
		end
		alreadyPressed = false
	end)

	GoBackButton.MouseButton1Click:Connect(function()
		TweenPlayerReverse:Play()
		local function playerCombatChildren()
			for i,v in pairs(playerCombat:GetChildren()) do
				if v:IsA("TextButton") then
					v.Visible = false
				end
				if v:IsA("TextLabel") then
					v.Visible = false
					v.TextTransparency = 1
				end
				if v:IsA("Frame") then
					v.Visible = false
				end
			end
		end
		playerCombatChildren()
		alreadyPressed = false
	end)

	---			local ShadowTween = TweenService:Create(
	--
end

----------------------------------------------------------------------------------------------------

local function LocalScript_14(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local TweenService = game:GetService("TweenService")
	local pressed = false
	local Players = game:GetService("Players")
	local player = Players.LocalPlayer
	local ReplicatedStorage = game:GetService("ReplicatedStorage")
	local Communication = ReplicatedStorage:WaitForChild("Communication")
	local RunService = game:GetService("RunService")
	local HttpService = game:GetService("HttpService")
	local MeleeTalk = Communication:WaitForChild("MeleeTalk")
	local MAXIMUM_DISTANCE = math.huge;
	local safety
	local torso = player.Character.Torso
	local bow_aura = nil
	local closestChar = nil
	local oldpos

	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local CombatTweenClick = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(244,63,63)}
	)
	local ShadowTweenClick = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(145,0,0)}

	)

	Combat.MouseButton1Click:Connect(function()
		local bg = Instance.new("BodyGyro", torso)
		local bv = Instance.new("BodyVelocity", torso)
		if not pressed then
			pressed = true
			CombatTween:Play()
			ShadowTween:Play()
			wait(0.25)
			Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
			Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)
			oldpos = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
			local function getClosestPlayer()
				local maximumDistance = 3000;
				local playerFound
				for i,target in pairs(Players:GetPlayers()) do
					if (target.Team ~= player.Team  and target.Team.Name ~= "Loading" and not table.find(_G.Protectee, target.UserId) and table.find(_G.Ostracize, target.UserId)) then
						local targetChar = target.Character or target.CharacterAdded:Wait()
						local clientChar = player.Character or player.CharacterAdded:Wait()
						if (targetChar) then
							local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")
							local targetHumanoid = targetChar:FindFirstChild("Humanoid")
							local clientHRP = clientChar:FindFirstChild("HumanoidRootPart")
							local clientHumanoid = clientChar:FindFirstChild("Humanoid")
							if(targetHRP and clientHRP and targetHumanoid and clientHumanoid and targetHumanoid.Health > 0 and clientHumanoid.Health > 0) then
								local x,y,z = targetHRP.Position.X, targetHRP.Position.Y, targetHRP.Position.Z
								local PLAYER_DISTANCE = target:DistanceFromCharacter(Vector3.new(609, 315, -92))
								if (PLAYER_DISTANCE < maximumDistance) then
									if (x > -2000 and x < 2000) then
										if (y > -1000 and y < 1000) then
											if (z > -2000 and z < 2000) then
												maximumDistance = PLAYER_DISTANCE
												playerFound = targetChar
											end
										end
									end
								end
							end
						end
					end
				end
				return playerFound
			end

			bow_aura = RunService.Heartbeat:Connect(function()
				for i,target in pairs(Players:GetPlayers()) do
					if (target.Team ~= player.Team and target.Name ~= player.Name and target.Team.Name ~= "Loading" and not table.find(_G.Protectee, target.UserId) and table.find(_G.Ostracize, target.UserId)) then
						local targetChar = target.Character or target.CharacterAdded:Wait()
						local clientChar = player.Character or player.CharacterAdded:Wait()
						if (targetChar) then
							local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")
							local targetHumanoid = targetChar:FindFirstChild("Humanoid")
							local clientHRP = clientChar:FindFirstChild("HumanoidRootPart")
							local clientHumanoid = clientChar:FindFirstChild("Humanoid")
							if(targetHRP and clientHRP and targetHumanoid and clientHumanoid and targetHumanoid.Health > 0 and clientHumanoid.Health > 0) then
								local playerTarget = getClosestPlayer()
								if (playerTarget) then
									local x,y,z = targetHRP.Position.X, targetHRP.Position.Y, targetHRP.Position.Z
									if (x > -2000 and x < 2000) then
										if (y > -1000 and y < 1000) then
											if (z > -2000 and z < 2000) then
												local playerCharacter = playerTarget
												local playerHRP = playerCharacter.HumanoidRootPart
												local playerPos = playerHRP.Position
												local args = {
													nil,
													tostring(playerTarget.Name),
													0,
													playerHRP.CFrame,
													playerPos,
													playerPos,
													'{"Z":-nan(ind),"X":-nan(ind),"Y":-nan(ind)}',
													nil,
													"faIse",
													false
												}
												Communication.RangedTalk:FireServer(unpack(args))
												task.spawn(function()
													local torso = game.Players.LocalPlayer.Character.Torso
													local bg = Instance.new("BodyGyro", torso)
													local bv = Instance.new("BodyVelocity", torso)
													game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(1077, 393, 1133687)
													game.Players.LocalPlayer.Character.Humanoid.PlatformStand = true
												end)
											end
										end
									end
								end
							end
						end
					end
				end
			end)

		else
			pressed = false
			ShadowTweenClick:Play()
			CombatTweenClick:Play()
			wait(0.25)
			Combat.BackgroundColor3 = Color3.fromRGB(244,63,63)
			Shadow.BackgroundColor3 = Color3.fromRGB(145,0,0)
			bow_aura:Disconnect()
			game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = oldpos
			for i,v in pairs(game.Players.LocalPlayer.Character.Torso:GetChildren()) do
				if v:IsA("BodyGyro") then
					v:Destroy()
				end
				if v:IsA("BodyVelocity") then
					v:Destroy()
				end
				game.Players.LocalPlayer.Character.Humanoid.PlatformStand = false
			end
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_15(script)


end

----------------------------------------------------------------------------------------------------

local function LocalScript_13(script)

	local Shadow = script.Parent
	local Combat = Shadow.Parent
	local TweenService = game:GetService("TweenService")
	local pressed = false
	local Players = game:GetService("Players")
	local player = Players.LocalPlayer
	local ReplicatedStorage = game:GetService("ReplicatedStorage")
	local Communication = ReplicatedStorage:WaitForChild("Communication")
	local RunService = game:GetService("RunService")
	local MAXIMUM_DISTANCE = math.huge;
	local bow_aura = nil
	local closestChar = nil
	local distance = 30
	local player = Players.LocalPlayer
	local character = player.Character or player.CharacterAdded:Wait()
	local Humanoid = character.Humanoid or character:FindFirstChild("Humanoid")
	local LOCAL_ROOT_PART = character:FindFirstChild("HumanoidRootPart") or character.HumanoidRootPart
	local MeleeTalk = Communication:WaitForChild("MeleeTalk")
	local RunService = game:GetService("RunService")
	local HttpService = game:GetService("HttpService")
	local MAXIMUM_DISTANCE = math.huge;
	local bow_aura
	local kill_aura
	local CLOSEST_CHARACTER
	local plr = game.Players.LocalPlayer



	local ShadowTween = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(13, 129, 0)}
	)


	local CombatTween = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(0, 180, 100)}
	)

	local CombatTweenClick = TweenService:Create(
		Combat,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(244,63,63)}
	)
	local ShadowTweenClick = TweenService:Create(
		Shadow,
		TweenInfo.new(0.25, Enum.EasingStyle.Sine, Enum.EasingDirection.In, 0, false, 0),
		{BackgroundColor3 = Color3.fromRGB(145,0,0)}

	)

	Combat.MouseButton1Click:Connect(function()
		if not pressed then
			pressed = true
			CombatTween:Play()
			ShadowTween:Play()
			wait(0.25)
			Combat.BackgroundColor3 = Color3.fromRGB(0, 180, 100)
			Shadow.BackgroundColor3 = Color3.fromRGB(13, 129, 0)
			local function getClosestPlayer()
				local maximumDistance = 5000
				local playerFound
				for i,target in pairs(Players:GetPlayers()) do
					if (target.Team ~= player.Team and target.Name ~= player.Name and target.Team.Name ~= "Loading" and not table.find(_G.Protectee,target.UserId) and table.find(_G.Ostracize, target.UserId)) then
						local targetChar = target.Character or target.CharacterAdded:Wait()
						local clientChar = player.Character or player.CharacterAdded:Wait()
						if (targetChar) then
							local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")
							local targetHumanoid = targetChar:FindFirstChild("Humanoid")
							local clientHRP = clientChar:FindFirstChild("HumanoidRootPart")
							local clientHumanoid = clientChar:FindFirstChild("Humanoid")
							if(targetHRP and clientHRP and targetHumanoid and clientHumanoid and targetHumanoid.Health > 0 and clientHumanoid.Health > 0) then
								local PLAYER_DISTANCE = player:DistanceFromCharacter(targetHRP.Position)
								if (PLAYER_DISTANCE < maximumDistance) then
									local x,y,z = targetHRP.Position.X, targetHRP.Position.Y, targetHRP.Position.Z
									if (x > -2000 and x < 2000) then
										if (y > -1000 and y < 1000) then
											if (z > -2000 and z < 2000) then
												maximumDistance = PLAYER_DISTANCE
												playerFound = targetChar
											end
										end
									end
								end
							end
						end
					end
				end
				return playerFound
			end

			bow_aura = RunService.Heartbeat:Connect(function()
				for i,target in pairs(Players:GetPlayers()) do
					if (target.Team ~= player.Team and target.Name ~= player.Name and target.Team.Name ~= "Loading" and not table.find(_G.Protectee,target.UserId) and table.find(_G.Ostracize, target.UserId)) then
						local targetChar = target.Character or target.CharacterAdded:Wait()
						local clientChar = player.Character or player.CharacterAdded:Wait()
						if (targetChar) then
							local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")
							local targetHumanoid = targetChar:FindFirstChild("Humanoid")
							local clientHRP = clientChar:FindFirstChild("HumanoidRootPart")
							local clientHumanoid = clientChar:FindFirstChild("Humanoid")
							if(targetHRP and clientHRP and targetHumanoid and clientHumanoid and targetHumanoid.Health > 0 and clientHumanoid.Health > 0) then
								local playerTarget = getClosestPlayer()
								if (playerTarget) then
									local x,y,z = targetHRP.Position.X, targetHRP.Position.Y, targetHRP.Position.Z
									if (x > -2000 and x < 2000) then
										if (y > -1000 and y < 1000) then
											if (z > -2000 and z < 2000) then
												local playerCharacter = playerTarget
												local playerHRP = playerCharacter.HumanoidRootPart
												local playerPos = playerHRP.Position
												local args = {
													nil,
													tostring(playerTarget.Name),
													0,
													playerHRP.CFrame,
													playerPos,
													playerPos,
													'{"Z":-nan(ind),"X":-nan(ind),"Y":-nan(ind)}',
													nil,
													"faIse",
													false
												}
												Communication.RangedTalk:FireServer(unpack(args))
											end
										end
									end
								end
							end
						end
					end
				end
			end)
		else
			pressed = false
			ShadowTweenClick:Play()
			CombatTweenClick:Play()
			wait(0.25)
			bow_aura:Disconnect()
			Combat.BackgroundColor3 = Color3.fromRGB(244,63,63)
			Shadow.BackgroundColor3 = Color3.fromRGB(145,0,0)
		end
	end)
end

----------------------------------------------------------------------------------------------------

local function LocalScript_41(script)

	if not getgenv().MTAPIMutex then loadstring(game:HttpGet("https://raw.githubusercontent.com/KikoTheDon/MT-Api-v2/main/__source/mt-api%20v2.lua", true))() end

	game:GetService("ReplicatedStorage").Communication.FallDamage:AddCallHook("FireServer", function() end)

	warn("Fall Damage Bypassed")
end
local function LocalScript_31(script)

	local shadow = script.Parent

	local Secret_Base = shadow.Parent

	Secret_Base.MouseButton1Click:Connect(function()
		local part = Instance.new("Part")
		part.Parent = game.Workspace
		part.Position = Vector3.new(247, 260, 413)
		part.Size = Vector3.new(5000, 1, 5000)
		part.Anchored = true
		game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(247, 265, 400)
	end)
end
local function LocalScript_33(script)

	local Frame = script.Parent

	Frame.ChoppedValue.chopval:GetPropertyChangedSignal("Value"):Connect(function()
		Frame.ChoppedValue.Text = Frame.ChoppedValue.chopval.Value
	end)
	Frame.MinedValue.mineval:GetPropertyChangedSignal("Value"):Connect(function()
		Frame.MinedValue.Text = Frame.MinedValue.mineval.Value
	end)
end
local function LocalScript_38(script)


	local DropHolder = game.Workspace.DropHolder

	local IgnoreParts = game.Workspace.Ignore.IgnoreParts

	local Walls = game.Workspace.Ignore.Walls

	local Barrier_Holder = game.Workspace.Ignore.barrier_holder
	for _,v in pairs(game:GetService("Players").LocalPlayer.PlayerScripts["Server Helpers"]:GetChildren()) do
		for _,c in pairs(getconnections(v:GetPropertyChangedSignal("Disabled"))) do
			c:Disable()
		end
		v.Disabled = true
	end

	for i,v in pairs(Barrier_Holder:GetChildren()) do
		v:Destroy()
	end
end


local function LocalScript_39(script)

	for _,v in pairs(game:GetService("Players").LocalPlayer.PlayerScripts["Server Helpers"]:GetChildren()) do
		for _,c in pairs(getconnections(v:GetPropertyChangedSignal("Disabled"))) do
			c:Disable()
		end
		v.Disabled = true
	end
end


local function LocalScript_32(script)

	local players = game:GetService("Players")
	local player = players.LocalPlayer
	local playerUserId = player.UserId
	local thumbType = Enum.ThumbnailType.HeadShot
	local thumbSize = Enum.ThumbnailSize.Size150x150
	local content, isReady = players:GetUserThumbnailAsync(playerUserId, thumbType, thumbSize)

	local imageLabel = script.Parent
	imageLabel.Image = content
	local playerName = imageLabel.playerName
	playerName.Text = "Welcome, "..players.LocalPlayer.Name
end

----------------------------------------------------------------------------------------------------

coroutine.wrap(LocalScript_1)(kmiWn.LocalScript_1)
coroutine.wrap(LocalScript_27)(kmiWn.LocalScript_27)
coroutine.wrap(LocalScript_28)(kmiWn.LocalScript_28)
coroutine.wrap(LocalScript_29)(kmiWn.LocalScript_29)
coroutine.wrap(LocalScript_30)(kmiWn.LocalScript_30)
coroutine.wrap(LocalScript_26)(kmiWn.LocalScript_26)
coroutine.wrap(LocalScript_24)(kmiWn.LocalScript_24)
coroutine.wrap(LocalScript_25)(kmiWn.LocalScript_25)
coroutine.wrap(LocalScript_23)(kmiWn.LocalScript_23)
coroutine.wrap(LocalScript_22)(kmiWn.LocalScript_22)
coroutine.wrap(LocalScript_31)(kmiWn.LocalScript_31)
coroutine.wrap(LocalScript_33)(kmiWn.LocalScript_33)
coroutine.wrap(LocalScript_38)(kmiWn.LocalScript_38)
coroutine.wrap(LocalScript_39)(kmiWn.LocalScript_39)
coroutine.wrap(LocalScript_32)(kmiWn.LocalScript_32)
coroutine.wrap(LocalScript_37)(kmiWn.LocalScript_37)
coroutine.wrap(LocalScript_35)(kmiWn.LocalScript_35)
coroutine.wrap(LocalScript_36)(kmiWn.LocalScript_36)
coroutine.wrap(LocalScript_34)(kmiWn.LocalScript_34)
coroutine.wrap(LocalScript_40)(kmiWn.LocalScript_40)
coroutine.wrap(LocalScript_21)(kmiWn.LocalScript_21)
coroutine.wrap(LocalScript_19)(kmiWn.LocalScript_19)
coroutine.wrap(LocalScript_6)(kmiWn.LocalScript_6)
coroutine.wrap(LocalScript_7)(kmiWn.LocalScript_7)
coroutine.wrap(LocalScript_8)(kmiWn.LocalScript_8)
coroutine.wrap(LocalScript_9)(kmiWn.LocalScript_9)
coroutine.wrap(LocalScript_5)(kmiWn.LocalScript_5)
coroutine.wrap(LocalScript_3)(kmiWn.LocalScript_3)
coroutine.wrap(LocalScript_4)(kmiWn.LocalScript_4)
coroutine.wrap(LocalScript_2)(kmiWn.LocalScript_2)
coroutine.wrap(LocalScript_20)(kmiWn.LocalScript_20)
coroutine.wrap(LocalScript_10)(kmiWn.LocalScript_10)
coroutine.wrap(LocalScript_12)(kmiWn.LocalScript_12)
coroutine.wrap(LocalScript_17)(kmiWn.LocalScript_17)
coroutine.wrap(LocalScript_18)(kmiWn.LocalScript_18)
coroutine.wrap(LocalScript_11)(kmiWn.LocalScript_11)
coroutine.wrap(LocalScript_16)(kmiWn.LocalScript_16)
coroutine.wrap(LocalScript_14)(kmiWn.LocalScript_14)
coroutine.wrap(LocalScript_15)(kmiWn.LocalScript_15)
coroutine.wrap(LocalScript_13)(kmiWn.LocalScript_13)
coroutine.wrap(LocalScript_41)(kmiWn.LocalScript_41)
